---
phase: 02-shared-debt-list-components
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/debt/shared-debt-list.tsx
  - web/src/components/debt/shared-debt-list.types.ts
  - web/src/components/debt/shared-debt-list.stories.tsx
  - web/src/__tests__/shared-debt-list.test.tsx
autonomous: true
requirements:
  - PH2-SDL-01
  - PH2-SDL-02
  - PH2-SDL-03
must_haves:
  truths:
    - Shared debt list UI is driven by a view-model contract, not domain entities.
    - One component supports mobile and desktop variants via explicit mode prop.
    - Loading, empty, and error/retry states are centralized in the shared component.
  artifacts:
    - path: web/src/components/debt/shared-debt-list.types.ts
      provides: canonical view-model and callback contract
      contains: "export interface DebtListItemVM"
    - path: web/src/components/debt/shared-debt-list.tsx
      provides: reusable responsive shared debt list component
      contains: "export function SharedDebtList"
    - path: web/src/__tests__/shared-debt-list.test.tsx
      provides: behavior coverage for mode, action visibility, and states
      contains: "describe('SharedDebtList'"
  key_links:
    - from: web/src/components/debt/shared-debt-list.tsx
      to: web/src/components/debt/shared-debt-list.types.ts
      via: component prop typing
      pattern: "from '@/components/debt/shared-debt-list.types'"
---

<objective>
Create the shared debt-list primitive and contract first.

Purpose: lock API + responsive rendering + standardized states so page migrations can run with low churn.
Output: reusable `SharedDebtList` + typed VM + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-shared-debt-list-components/02-CONTEXT.md
@web/src/components/CompactEntityTable.tsx
@web/src/pages/friends.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define shared debt list view-model and callback capabilities</name>
  <files>
    web/src/components/debt/shared-debt-list.types.ts
  </files>
  <read_first>
    - .planning/phases/02-shared-debt-list-components/02-CONTEXT.md
    - web/src/components/CompactEntityTable.tsx
    - web/src/lib/api.ts
  </read_first>
  <action>
Create `web/src/components/debt/shared-debt-list.types.ts` with explicit concrete types:
- `type DebtListMode = 'auto' | 'mobile' | 'desktop'`
- `type DebtListView = 'owner' | 'public-friend' | 'public-group'`
- `type DebtStatusTone = 'default' | 'secondary' | 'outline'`
- `interface DebtListStatusVM { label: string; tone: DebtStatusTone }`
- `interface DebtListItemVM { id: string; title: string; subtitle: string; amount: number; currency: string; status: DebtListStatusVM; meta?: string; canOpen?: boolean; canConfirm?: boolean; canDelete?: boolean; canCopy?: boolean; }`
- `interface SharedDebtListProps { mode?: DebtListMode; view: DebtListView; items: DebtListItemVM[]; loading?: boolean; error?: string | null; emptyTitle?: string; emptyHint?: string; primaryActionLabel?: string; onRetry?: () => void; onOpen?: (id: string) => void; onConfirm?: (id: string) => void; onDelete?: (id: string) => void; onCopy?: (id: string) => void; }`
Do not import backend/domain entities into this file.
  </action>
  <acceptance_criteria>
    - `web/src/components/debt/shared-debt-list.types.ts` exists
    - file contains `export interface DebtListItemVM`
    - file contains `mode?: DebtListMode`
    - file contains `onOpen?: (id: string) => void`
    - file contains `onConfirm?: (id: string) => void`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared debt list component with standardized states</name>
  <files>
    web/src/components/debt/shared-debt-list.tsx
  </files>
  <read_first>
    - web/src/components/CompactEntityTable.tsx
    - web/src/components/ui/money-value.tsx
    - web/src/components/ui/button.tsx
    - web/src/components/ui/badge.tsx
    - web/src/components/debt/shared-debt-list.types.ts
  </read_first>
  <action>
Create `web/src/components/debt/shared-debt-list.tsx` exporting `SharedDebtList(props: SharedDebtListProps)`.
Implement concrete behavior:
- `loading=true`: render exactly 3 skeleton rows (`animate-pulse`, bordered blocks)
- `error` present: render inline error panel with text + retry button when `onRetry` exists
- `items.length===0` and not loading/error: render empty block with `emptyTitle` and `emptyHint`
- render mobile card layout for `mode='mobile'` and desktop table layout for `mode='desktop'`; `mode='auto'` keeps `sm:hidden` + `hidden sm:block` split
- row click triggers `onOpen(id)` only when `item.canOpen !== false` and callback exists
- action buttons rendered strictly by capability callbacks and item flags:
  - confirm button when `onConfirm` and `item.canConfirm`
  - delete button when `onDelete` and `item.canDelete`
  - copy button when `onCopy` and `item.canCopy`
- money rendered with `<MoneyValue amount={item.amount} currency={item.currency} showSign="auto" tone="auto" />`
  </action>
  <acceptance_criteria>
    - file contains `export function SharedDebtList`
    - file contains `mode = 'auto'`
    - file contains `onRetry`
    - file contains `animate-pulse`
    - file contains `MoneyValue`
    - file contains `onOpen?.(item.id)`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add focused tests for state and capability rendering</name>
  <files>
    web/src/__tests__/shared-debt-list.test.tsx
  </files>
  <read_first>
    - web/src/__tests__/dashboard.test.tsx
    - web/src/__tests__/verdict.test.tsx
    - web/src/components/debt/shared-debt-list.tsx
  </read_first>
  <action>
Create `web/src/__tests__/shared-debt-list.test.tsx` with concrete cases:
- loading state renders 3 skeleton rows
- empty state renders provided `emptyTitle` and `emptyHint`
- error state renders error text and Retry button when `onRetry` passed
- action buttons appear only for enabled capabilities (`canConfirm/canDelete/canCopy` + callback present)
- row click calls `onOpen` for openable rows only
  </action>
  <acceptance_criteria>
    - file contains `describe('SharedDebtList'`
    - file contains `loading state`
    - file contains `error state`
    - file contains `onOpen`
    - `cd web && npm test -- --run src/__tests__/shared-debt-list.test.tsx` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd web && npm test -- --run src/__tests__/shared-debt-list.test.tsx
cd web && npm run build
```
</verification>

<success_criteria>
- [ ] Shared debt-list VM contract is explicit and reusable.
- [ ] Responsive/mobile+desktop rendering is centralized in one component.
- [ ] Loading/empty/error-retry states are standardized.
- [ ] Capability-based actions are test-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-debt-list-components/02-01-SUMMARY.md`.
</output>
