---
phase: 02-shared-debt-list-components
plan: "03"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - web/src/pages/friend-public.tsx
  - web/src/pages/group-public.tsx
  - web/src/lib/api.ts
  - internal/handler/peer_debt.go
  - internal/service/peer_debt.go
  - internal/repo/sqlite/peer_debt.go
  - internal/handler/public.go
  - internal/service/group_event.go
  - internal/repo/sqlite/group_event.go
autonomous: true
requirements:
  - PH2-SDL-06
  - PH2-SDL-07
must_haves:
  truths:
    - Public surfaces consume shared debt list UI with explicit capability matrix.
    - Public pages remain read-only except host-specific payment confirmation toggle in friend-public.
    - Confirm-paid behavior is reversible and applies strict rollback of side effects.
  artifacts:
    - path: web/src/pages/friend-public.tsx
      provides: friend-public integration with shared list and host-only toggle
      contains: "<SharedDebtList"
    - path: internal/service/peer_debt.go
      provides: reversible confirm toggle service method
      contains: "ToggleInstallmentConfirmation"
    - path: internal/service/group_event.go
      provides: reversible participant confirmation behavior
      contains: "ToggleParticipantConfirmation"
---

<objective>
Migrate public debt surfaces to shared list and implement reversible confirm semantics required by context.

Purpose: enforce permission matrix + keep public read-only defaults while allowing host-only confirm toggle in friend-public.
Output: shared list on public pages + backend/API toggle endpoints with rollback behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-shared-debt-list-components/02-CONTEXT.md
@web/src/pages/friend-public.tsx
@web/src/pages/group-public.tsx
@internal/handler/public.go
@internal/service/peer_debt.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate shared debt list in public pages with strict capability matrix</name>
  <files>
    web/src/pages/friend-public.tsx
    web/src/pages/group-public.tsx
    web/src/components/debt/debt-list-mappers.ts
  </files>
  <read_first>
    - web/src/pages/friend-public.tsx
    - web/src/pages/group-public.tsx
    - web/src/components/debt/shared-debt-list.tsx
    - .planning/phases/02-shared-debt-list-components/02-CONTEXT.md
  </read_first>
  <action>
Replace public list rendering blocks with `SharedDebtList` and map data through VM mappers.
Concrete capability rules:
- `group-public` items: `canOpen=true`, `canConfirm=false`, `canDelete=false`, `canCopy=false`
- `friend-public` default: same read-only matrix
- `friend-public` host exception: enable `canConfirm=true` only for host-eligible participant/payment rows
Use clear view labels (`view='public-group'` and `view='public-friend'`) and pass public-appropriate empty/error copy.
  </action>
  <acceptance_criteria>
    - `web/src/pages/friend-public.tsx` contains `<SharedDebtList`
    - `web/src/pages/group-public.tsx` contains `<SharedDebtList`
    - files contain explicit `canConfirm` gating
    - group-public file contains no delete/confirm mutation wiring
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add reversible confirm toggle endpoints and service methods</name>
  <files>
    internal/handler/peer_debt.go
    internal/service/peer_debt.go
    internal/repo/sqlite/peer_debt.go
    internal/handler/public.go
    internal/service/group_event.go
    internal/repo/sqlite/group_event.go
    web/src/lib/api.ts
  </files>
  <read_first>
    - internal/handler/peer_debt.go
    - internal/service/peer_debt.go
    - internal/repo/sqlite/peer_debt.go
    - internal/handler/public.go
    - internal/service/group_event.go
    - web/src/lib/api.ts
  </read_first>
  <action>
Implement reversible toggle semantics with concrete API contract changes:
- peer debt owner flow:
  - add `POST /api/peer-debts/:id/confirm-toggle` (or `PATCH .../confirm` with explicit toggle payload) in handler/routes
  - service method `ToggleInstallmentConfirmation(id uuid.UUID)`
  - repo method that toggles confirmation atomically:
    - non-installment: `is_confirmed = NOT is_confirmed`
    - installment: increment on ON, decrement on OFF (never below 0, never above total_installments)
- public friend host flow:
  - add `POST /public/friend/:token/groups/:eventID/participants/:friendID/confirm-toggle`
  - service/repo toggles participant `is_confirmed` and reverses any derived financial/projection side effects currently applied by confirmation path
- frontend API:
  - add `toggleDebtConfirm(id: string)`
  - add `togglePublicFriendGroupPayment(token, eventId, friendId)`
Keep old confirm endpoints for compatibility only if currently consumed; otherwise migrate callers to toggle endpoints.
  </action>
  <acceptance_criteria>
    - handler file contains `confirm-toggle`
    - service file contains `ToggleInstallmentConfirmation`
    - repo file contains atomic toggle SQL (contains `CASE` and confirmation fields)
    - `web/src/lib/api.ts` contains `toggleDebtConfirm`
    - `web/src/lib/api.ts` contains `togglePublicFriendGroupPayment`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add rollback-focused tests for toggle ON/OFF parity</name>
  <files>
    internal/service/peer_debt_test.go
    internal/service/group_event_test.go
    web/src/__tests__/friend-public.test.tsx
  </files>
  <read_first>
    - internal/service/pay_schedule_test.go
    - internal/handler/check_test.go
    - web/src/__tests__/dashboard.test.tsx
    - web/src/pages/friend-public.tsx
  </read_first>
  <action>
Add concrete tests that prove reversible semantics:
- peer debt service test:
  - toggle ON changes state as expected
  - toggle OFF returns to prior confirmation state and prior paid_installments/accounted effects
- group event service test:
  - participant confirm toggle ON/OFF is symmetric
  - OFF path restores prior aggregate state used in projections
- friend-public UI test:
  - host-visible row shows confirm toggle button
  - non-host rows remain read-only
  - toggling calls new API function and refreshes list
  </action>
  <acceptance_criteria>
    - `internal/service/peer_debt_test.go` contains `ToggleInstallmentConfirmation`
    - `internal/service/group_event_test.go` contains `ToggleParticipantConfirmation`
    - `web/src/__tests__/friend-public.test.tsx` contains `togglePublicFriendGroupPayment`
    - `go test ./internal/service/...` exits 0
    - `cd web && npm test -- --run src/__tests__/friend-public.test.tsx` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
go test ./internal/service/...
cd web && npm test -- --run src/__tests__/friend-public.test.tsx
cd web && npm run build
```
</verification>

<success_criteria>
- [ ] Public pages use shared debt list with explicit permission gating.
- [ ] Host-only confirm toggle is available only in allowed friend-public context.
- [ ] Confirm toggle supports ON/OFF with strict rollback semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-debt-list-components/02-03-SUMMARY.md`.
</output>
