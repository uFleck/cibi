---
phase: 02-shared-debt-list-components
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - web/src/pages/friends.tsx
  - web/src/components/debt/debt-list-mappers.ts
  - web/src/components/AppModal.tsx
  - web/src/components/debt/friend-debt-details-modal.tsx
autonomous: true
requirements:
  - PH2-SDL-04
  - PH2-SDL-05
must_haves:
  truths:
    - Friends owner debt section consumes the shared debt list via VM mapper.
    - Domain logic and mutations remain page-owned; shared component stays presentational.
    - Row-open behavior is modal-based, with full-screen presentation on mobile.
  artifacts:
    - path: web/src/components/debt/debt-list-mappers.ts
      provides: owner-surface VM mapping from existing debt entities
      contains: "export function mapFriendDebtsToVM"
    - path: web/src/pages/friends.tsx
      provides: migration from duplicated debt list rendering to SharedDebtList
      contains: "<SharedDebtList"
    - path: web/src/components/debt/friend-debt-details-modal.tsx
      provides: standardized modal detail surface
      contains: "export function FriendDebtDetailsModal"
---

<objective>
Migrate owner (friends page) debt-list usage to the new shared component and enforce modal-open flow.

Purpose: remove duplicated list rendering while preserving owner mutation behavior and permission checks.
Output: friends page integrated with SharedDebtList + mobile full-screen modal details.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-shared-debt-list-components/02-CONTEXT.md
@web/src/pages/friends.tsx
@web/src/components/AppModal.tsx
@web/src/components/debt/shared-debt-list.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit owner VM mapper with concrete field transforms</name>
  <files>
    web/src/components/debt/debt-list-mappers.ts
  </files>
  <read_first>
    - web/src/pages/friends.tsx
    - web/src/lib/api.ts
    - web/src/components/debt/shared-debt-list.types.ts
  </read_first>
  <action>
Create `web/src/components/debt/debt-list-mappers.ts` with:
- `mapFriendDebtsToVM(input: PeerDebtResponse[]): DebtListItemVM[]`
Concrete mapping rules:
- `title` = `description`
- `subtitle` = next due date string (current `calculateNextPayDate` logic output)
- `amount` = existing display amount logic currently used in friends page (`getDisplayAmount` equivalent)
- `currency` = `'BRL'`
- `status` uses current status labeling logic (`debtStatus` equivalent)
- capability flags:
  - `canOpen=true`
  - `canConfirm = !is_confirmed`
  - `canDelete=true`
  - `canCopy=false`
Do not call mutations in mapper.
  </action>
  <acceptance_criteria>
    - file contains `export function mapFriendDebtsToVM`
    - file contains `canConfirm`
    - file contains `currency: 'BRL'`
    - file contains `DebtListItemVM[]`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace duplicated friends debt rendering with SharedDebtList</name>
  <files>
    web/src/pages/friends.tsx
  </files>
  <read_first>
    - web/src/pages/friends.tsx
    - web/src/components/debt/shared-debt-list.tsx
    - web/src/components/debt/debt-list-mappers.ts
  </read_first>
  <action>
Update `web/src/pages/friends.tsx` concrete integrations:
- remove current duplicated mobile card block + desktop table block for friend debts
- import and render `<SharedDebtList ... />` in debt section
- pass mapped `items={mapFriendDebtsToVM(debts)}`
- wire callbacks:
  - `onOpen` sets selected debt id for details modal
  - `onConfirm` triggers existing `confirmMutation`
  - `onDelete` opens existing delete confirmation flow
- keep mutation ownership (`useMutation`, toasts, cache invalidation) in page
- keep account/friend query ownership in page
- for empty state pass text: title `No debts recorded`, hint `Add Debt to start tracking`
- for error state pass query error message and retry via `refetch`
  </action>
  <acceptance_criteria>
    - `web/src/pages/friends.tsx` contains `<SharedDebtList`
    - file no longer contains both old debt-rendering containers (`sm:hidden flex flex-col gap-2` and `hidden sm:block border rounded-md overflow-x-auto`) for debts section
    - file contains `mapFriendDebtsToVM(`
    - file contains `onConfirm`
    - file contains `onDelete`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Standardize details open flow to modal with mobile full-screen behavior</name>
  <files>
    web/src/components/debt/friend-debt-details-modal.tsx
    web/src/components/AppModal.tsx
    web/src/pages/friends.tsx
  </files>
  <read_first>
    - web/src/components/AppModal.tsx
    - web/src/pages/friends.tsx
    - web/src/components/ui/dialog.tsx
  </read_first>
  <action>
Implement modal standardization with concrete behavior:
- create `FriendDebtDetailsModal` component using `AppModal`
- in mobile viewport apply full-screen shell classes (`h-screen w-screen max-w-none rounded-none`) while preserving desktop centered dialog
- row open from SharedDebtList always opens this modal (not route transition)
- modal displays debt title, status, next date, amount, and owner actions already allowed by callbacks
- keep destructive confirms/deletes behind existing confirm dialog behavior
  </action>
  <acceptance_criteria>
    - `web/src/components/debt/friend-debt-details-modal.tsx` exists
    - file contains `export function FriendDebtDetailsModal`
    - file contains `h-screen`
    - `web/src/pages/friends.tsx` opens modal from `onOpen`
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd web && npm run build
cd web && npm test -- --run src/__tests__/shared-debt-list.test.tsx
```
</verification>

<success_criteria>
- [ ] Friends owner debt UI now uses shared list component.
- [ ] Page keeps business/mutation logic ownership (UI-only extraction respected).
- [ ] Row-open interaction is modal-based and mobile full-screen.
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-debt-list-components/02-02-SUMMARY.md`.
</output>
