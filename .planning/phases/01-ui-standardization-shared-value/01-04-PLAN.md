---
phase: 01-ui-standardization-shared-value
plan: "04"
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - web/src/pages/transactions.tsx
  - web/src/pages/accounts.tsx
autonomous: true
requirements:
  - UI-STD-01
  - UI-STD-02
  - UI-STD-03
  - UI-STD-05
must_haves:
  truths:
    - Transactions and accounts pages stop rendering money with `.toFixed(2)` in UI output.
    - Mobile and desktop branches show identical sign/tone semantics for the same amount.
    - Currency source is explicit and derived from selected/current account state.
  artifacts:
    - path: web/src/pages/transactions.tsx
      provides: Table/card amount rendering via MoneyValue
      contains: "<MoneyValue"
    - path: web/src/pages/accounts.tsx
      provides: Account balance and schedule amount rendering via MoneyValue
      contains: "<MoneyValue"
  key_links:
    - from: web/src/pages/transactions.tsx
      to: accounts query data
      via: resolved current account currency
      pattern: "accounts.find"
    - from: web/src/pages/accounts.tsx
      to: schedule modal account
      via: schedule currency resolution
      pattern: "scheduleModalAccountId"
---

<objective>
Migrate the two highest-churn CRUD pages (accounts + transactions) to shared value rendering.

Purpose: Eliminate duplicated sign/format logic in mobile+desktop branches.
Output: No `.toFixed(2)` display in these pages; explicit currency flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ui-standardization-shared-value/01-02-PLAN.md
@web/src/pages/transactions.tsx
@web/src/pages/accounts.tsx
@web/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace transaction amount rendering in mobile+desktop branches</name>
  <files>
    web/src/pages/transactions.tsx
  </files>
  <read_first>
    - web/src/components/ui/money-value.tsx
    - web/src/pages/transactions.tsx
    - web/src/lib/api.ts
  </read_first>
  <action>
In `transactions.tsx`:
1) add `MoneyValue` import.
2) resolve currency once near `currentAccountId`:
   - `const currentAccountCurrency = accounts.find(a => a.id === currentAccountId)?.currency ?? 'BRL'`
3) replace both amount render points:
   - mobile card amount block (`txn.amount >= 0 ? '+' : ''}{txn.amount.toFixed(2)}`)
   - desktop table amount block (`txn.amount >= 0 ? '+' : ''}{txn.amount.toFixed(2)}`)
with:
`<MoneyValue amount={txn.amount} currency={currentAccountCurrency} showSign='always' tone='auto' className='font-semibold' />` (mobile)
`<MoneyValue amount={txn.amount} currency={currentAccountCurrency} showSign='always' tone='auto' className='font-medium' />` (desktop)

Keep existing sort/filter/query/mutation logic untouched.
  </action>
  <acceptance_criteria>
    - `web/src/pages/transactions.tsx` contains `const currentAccountCurrency = accounts.find(`
    - file contains at least 2 `<MoneyValue` usages
    - file no longer contains `{txn.amount >= 0 ? '+' : ''}{txn.amount.toFixed(2)}`
    - file no longer contains `txn.amount.toFixed(2)` in rendered JSX
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace account/schedule amount rendering and wire explicit currency</name>
  <files>
    web/src/pages/accounts.tsx
  </files>
  <read_first>
    - web/src/components/ui/money-value.tsx
    - web/src/pages/accounts.tsx
  </read_first>
  <action>
In `accounts.tsx`:
1) add `MoneyValue` import.
2) replace account balance output in both mobile and desktop tables:
   - `account.current_balance.toFixed(2)` -> `<MoneyValue amount={account.current_balance} currency={account.currency} showSign='never' tone='auto' ... />`
3) for schedule rows (modal mobile + desktop), avoid BRL default drift:
   - define `const scheduleCurrency = accounts.find(a => a.id === scheduleModalAccountId)?.currency ?? 'BRL'`
   - replace `+{formatMoney(ps.amount)}` with `<MoneyValue amount={ps.amount} currency={scheduleCurrency} showSign='always' tone='positive' ... />`

Do not modify form parsing lines that use `.toFixed(2)` for input state (`ps.amount.toFixed(2)` in edit form setup is allowed).
  </action>
  <acceptance_criteria>
    - `web/src/pages/accounts.tsx` contains `const scheduleCurrency = accounts.find(`
    - file contains `<MoneyValue` in account balance mobile + desktop sections
    - file no longer contains `account.current_balance.toFixed(2)` in rendered JSX
    - schedule amount rows no longer contain `+{formatMoney(ps.amount)}`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Enforce no raw display decimals/sign ternaries in accounts+transactions</name>
  <files>
    web/src/pages/accounts.tsx
    web/src/pages/transactions.tsx
  </files>
  <read_first>
    - web/src/pages/accounts.tsx
    - web/src/pages/transactions.tsx
  </read_first>
  <action>
Run and satisfy these checks:
- `rg -n "\.toFixed\(2\)" web/src/pages/accounts.tsx web/src/pages/transactions.tsx`
- `rg -n "amount >= 0 \? '\+' : ''" web/src/pages/accounts.tsx web/src/pages/transactions.tsx`
- `cd web && npm run build`

Acceptable `.toFixed(2)` usage: form-state initialization only (e.g., `ps.amount.toFixed(2)` assigned to input draft), not JSX display output.
  </action>
  <acceptance_criteria>
    - no `amount >= 0 ? '+' : ''` matches in accounts/transactions pages
    - no JSX display usage of `.toFixed(2)` for rendered money values
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
rg -n "\.toFixed\(2\)|amount >= 0 \? '\+' : ''" web/src/pages/accounts.tsx web/src/pages/transactions.tsx
cd web && npm run build
```
</verification>

<success_criteria>
- [ ] Accounts and transactions amount rendering uses `MoneyValue`.
- [ ] Duplicate sign logic removed from both mobile and desktop branches.
- [ ] Currency passed explicitly from account context/state.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-standardization-shared-value/01-04-SUMMARY.md`.
</output>
