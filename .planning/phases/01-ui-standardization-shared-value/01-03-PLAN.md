---
phase: 01-ui-standardization-shared-value
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - web/src/components/StatCards.tsx
  - web/src/components/ObligationsList.tsx
  - web/src/components/PayScheduleList.tsx
  - web/src/components/ProjectionWidget.tsx
  - web/src/components/FriendLedgerWidget.tsx
  - web/src/components/CheckWidget.tsx
autonomous: true
requirements:
  - UI-STD-01
  - UI-STD-02
  - UI-STD-03
  - UI-STD-04
  - UI-STD-05
must_haves:
  truths:
    - Dashboard widgets use one shared value primitive for all monetary numbers.
    - Manual `+`/`-` concatenation is removed from migrated dashboard components.
    - Currency is explicit on account-scoped widgets.
  artifacts:
    - path: web/src/components/ProjectionWidget.tsx
      provides: Projection rows render via MoneyValue
      contains: "<MoneyValue"
    - path: web/src/components/PayScheduleList.tsx
      provides: Incoming totals/rows render via MoneyValue
      contains: "<MoneyValue"
    - path: web/src/components/CheckWidget.tsx
      provides: Verdict value rows render via MoneyValue
      contains: "<MoneyValue"
  key_links:
    - from: web/src/components/StatCards.tsx
      to: web/src/components/ui/money-value.tsx
      via: component import
      pattern: "from '@/components/ui/money-value'"
    - from: web/src/components/ProjectionWidget.tsx
      to: account.currency
      via: MoneyValue currency prop
      pattern: "currency={account.currency}"
---

<objective>
Migrate dashboard/value-heavy widgets to `MoneyValue` without changing calculations.

Purpose: Make high-traffic UI consistent first.
Output: All dashboard component amount displays standardized.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ui-standardization-shared-value/01-02-PLAN.md
@web/src/components/StatCards.tsx
@web/src/components/ObligationsList.tsx
@web/src/components/PayScheduleList.tsx
@web/src/components/ProjectionWidget.tsx
@web/src/components/FriendLedgerWidget.tsx
@web/src/components/CheckWidget.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize simple dashboard value rows/cards</name>
  <files>
    web/src/components/StatCards.tsx
    web/src/components/ObligationsList.tsx
    web/src/components/PayScheduleList.tsx
  </files>
  <read_first>
    - web/src/components/ui/money-value.tsx
    - web/src/components/StatCards.tsx
    - web/src/components/ObligationsList.tsx
    - web/src/components/PayScheduleList.tsx
  </read_first>
  <action>
Replace direct `formatMoney(...)` display spans with `<MoneyValue ... />` in:
- `StatCards.tsx` for `Balance`, `Reserved`, `Liquid`
- `ObligationsList.tsx` per-row amount + `Total reserved`
- `PayScheduleList.tsx` per-row amount + `Total incoming`

Use concrete props:
- In pay schedule rows: `showSign='always'` and `tone='positive'`.
- In obligations totals: `tone='negative'` and `showSign='never'`.
- Always pass currency from existing source (`account.currency` or `currency` prop).
  </action>
  <acceptance_criteria>
    - each file imports `MoneyValue` from `@/components/ui/money-value`
    - `web/src/components/PayScheduleList.tsx` no longer contains `+{formatMoney(`
    - `web/src/components/StatCards.tsx` contains at least 3 `<MoneyValue` usages
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Standardize complex dashboard widgets (projection, friend ledger, check)</name>
  <files>
    web/src/components/ProjectionWidget.tsx
    web/src/components/FriendLedgerWidget.tsx
    web/src/components/CheckWidget.tsx
  </files>
  <read_first>
    - web/src/components/ui/money-value.tsx
    - web/src/components/ProjectionWidget.tsx
    - web/src/components/FriendLedgerWidget.tsx
    - web/src/components/CheckWidget.tsx
  </read_first>
  <action>
In `ProjectionWidget.tsx`, replace:
- `+{formatMoney(incoming, account.currency)}`
- `-{formatMoney(nextObligations, account.currency).replace('-', '')}`
with `MoneyValue` using exact props:
- incoming: `tone='positive'`, `showSign='always'`, `currency={account.currency}`
- obligations: `tone='negative'`, `showSign='always'`, `amount={-nextObligations}`, `currency={account.currency}`

In `FriendLedgerWidget.tsx`, migrate all monetary spans (`total_owed_to_user`, `total_user_owes`, `net`, breakdown totals) to `MoneyValue` and keep current visual polarity.
In `CheckWidget.tsx`, migrate `purchasing_power` and `buffer_remaining` lines to `MoneyValue` (use existing account fallback behavior if currency is unavailable).
  </action>
  <acceptance_criteria>
    - `web/src/components/ProjectionWidget.tsx` no longer contains `+{formatMoney(`
    - `web/src/components/ProjectionWidget.tsx` no longer contains `.replace('-', '')`
    - `web/src/components/FriendLedgerWidget.tsx` contains `<MoneyValue` for summary + net rows
    - `web/src/components/CheckWidget.tsx` contains `<MoneyValue` for both value lines
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Run focused regression + grep checks for dashboard scope</name>
  <files>
    web/src/components/StatCards.tsx
    web/src/components/ObligationsList.tsx
    web/src/components/PayScheduleList.tsx
    web/src/components/ProjectionWidget.tsx
    web/src/components/FriendLedgerWidget.tsx
    web/src/components/CheckWidget.tsx
  </files>
  <read_first>
    - web/src/__tests__/dashboard.test.tsx
    - web/src/__tests__/verdict.test.tsx
  </read_first>
  <action>
Run dashboard/verdict tests and grep guards:
- `cd web && npm test -- --run src/__tests__/dashboard.test.tsx src/__tests__/verdict.test.tsx`
- `rg -n "\+\{formatMoney\(" web/src/components/StatCards.tsx web/src/components/ObligationsList.tsx web/src/components/PayScheduleList.tsx web/src/components/ProjectionWidget.tsx web/src/components/FriendLedgerWidget.tsx web/src/components/CheckWidget.tsx`
- `rg -n "amount >= 0 \? '\+' : ''"` with same file set.

If grep finds matches in migrated files, replace them with `MoneyValue` props.
  </action>
  <acceptance_criteria>
    - dashboard/verdict test command exits 0
    - grep for `+{formatMoney(` returns no matches in listed dashboard files
    - grep for `amount >= 0 ? '+' : ''` returns no matches in listed dashboard files
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd web && npm test -- --run src/__tests__/dashboard.test.tsx src/__tests__/verdict.test.tsx
rg -n "\+\{formatMoney\(|amount >= 0 \? '\+' : ''" web/src/components/StatCards.tsx web/src/components/ObligationsList.tsx web/src/components/PayScheduleList.tsx web/src/components/ProjectionWidget.tsx web/src/components/FriendLedgerWidget.tsx web/src/components/CheckWidget.tsx
cd web && npm run build
```
</verification>

<success_criteria>
- [ ] Dashboard amount rendering uses `MoneyValue`.
- [ ] No manual sign concatenation remains in migrated dashboard files.
- [ ] Dashboard/verdict tests remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-standardization-shared-value/01-03-SUMMARY.md`.
</output>
