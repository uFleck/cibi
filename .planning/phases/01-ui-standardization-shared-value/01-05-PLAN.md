---
phase: 01-ui-standardization-shared-value
plan: "05"
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - web/src/components/CompactEntityTable.tsx
  - web/src/components/ParticipantEditor.tsx
  - web/src/pages/friends.tsx
  - web/src/pages/friend-public.tsx
  - web/src/pages/group-public.tsx
autonomous: true
requirements:
  - UI-STD-01
  - UI-STD-03
  - UI-STD-04
  - UI-STD-05
must_haves:
  truths:
    - Friends/public value displays follow the same sign/tone contract as authenticated pages.
    - Compact list/table components can render rich value nodes (not string-only), preventing formatter regressions.
    - Public pages use explicit fallback currency instead of implicit formatter default.
  artifacts:
    - path: web/src/components/CompactEntityTable.tsx
      provides: secondary field accepts ReactNode for mixed text + MoneyValue
      contains: "secondary: React.ReactNode"
    - path: web/src/pages/friend-public.tsx
      provides: Debt/group totals rendered via MoneyValue
      contains: "<MoneyValue"
    - path: web/src/pages/group-public.tsx
      provides: Participant share rows rendered via MoneyValue
      contains: "<MoneyValue"
  key_links:
    - from: web/src/pages/friends.tsx
      to: web/src/components/CompactEntityTable.tsx
      via: secondary ReactNode payload
      pattern: "secondary: ("
    - from: web/src/pages/friend-public.tsx
      to: web/src/components/ui/money-value.tsx
      via: import + usage
      pattern: "from '@/components/ui/money-value'"
---

<objective>
Migrate friends and public-share surfaces to shared value rendering, including compact table plumbing.

Purpose: Finish cross-surface consistency and remove string-based money formatting bottlenecks.
Output: Public/friends value UIs standardized with explicit fallback currency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ui-standardization-shared-value/01-02-PLAN.md
@web/src/components/CompactEntityTable.tsx
@web/src/components/ParticipantEditor.tsx
@web/src/pages/friends.tsx
@web/src/pages/friend-public.tsx
@web/src/pages/group-public.tsx
@web/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable rich value nodes in compact table API</name>
  <files>
    web/src/components/CompactEntityTable.tsx
    web/src/pages/friends.tsx
    web/src/pages/group-public.tsx
  </files>
  <read_first>
    - web/src/components/CompactEntityTable.tsx
    - web/src/pages/friends.tsx
    - web/src/pages/group-public.tsx
  </read_first>
  <action>
Update `CompactEntityTableItem` contract:
- change `secondary: string` -> `secondary: React.ReactNode`
- keep backward compatibility for string callers.

Then update call sites in `friends.tsx` and `group-public.tsx` to pass JSX fragments where money appears, e.g.:
- friends row secondary: open-count text + `<MoneyValue amount={overview.total} currency='BRL' showSign='auto' tone='auto' />`
- group participant secondary: `<MoneyValue amount={p.share_amount} currency='BRL' showSign='never' tone='auto' />` + status text.

Do not change copy strings (`open`, `total`, `Confirmed`, `Pending`).
  </action>
  <acceptance_criteria>
    - `web/src/components/CompactEntityTable.tsx` contains `secondary: React.ReactNode`
    - `web/src/pages/friends.tsx` contains `secondary: (`
    - `web/src/pages/group-public.tsx` contains `secondary: (`
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrate friend/public/participant amount fields to MoneyValue</name>
  <files>
    web/src/components/ParticipantEditor.tsx
    web/src/pages/friends.tsx
    web/src/pages/friend-public.tsx
    web/src/pages/group-public.tsx
  </files>
  <read_first>
    - web/src/components/ui/money-value.tsx
    - web/src/components/ParticipantEditor.tsx
    - web/src/pages/friend-public.tsx
    - web/src/pages/friends.tsx
    - web/src/pages/group-public.tsx
  </read_first>
  <action>
Add `MoneyValue` import and migrate all numeric money displays in these files.

Concrete replacements:
- `ParticipantEditor.tsx`: replace `formatMoney(equalShare)` in mobile and desktop sections with `MoneyValue amount={equalShare} currency='BRL' showSign='never' tone='auto'`.
- `friend-public.tsx`: replace group shares, debt rows, totals, installment tooltip amounts, payment history amounts with `MoneyValue`; set `tone='auto'` for signed balances, `showSign='never'` for neutral shares.
- `friends.tsx`: replace money fragments in compact table secondary strings with `MoneyValue` JSX.
- `group-public.tsx`: replace total and participant share displays with `MoneyValue`.

Use explicit fallback `currency='BRL'` for public/friends surfaces because API payloads currently have no currency field.
  </action>
  <acceptance_criteria>
    - each listed file imports `MoneyValue`
    - `web/src/components/ParticipantEditor.tsx` no longer contains `formatMoney(equalShare)`
    - `web/src/pages/friend-public.tsx` contains at least 8 `<MoneyValue` usages
    - `web/src/pages/group-public.tsx` no longer contains `formatMoney(data.total_amount)`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Remove leftover ad-hoc tone/sign code in friends/public scope</name>
  <files>
    web/src/pages/friends.tsx
    web/src/pages/friend-public.tsx
    web/src/pages/group-public.tsx
    web/src/components/ParticipantEditor.tsx
  </files>
  <read_first>
    - web/src/pages/friend-public.tsx
    - web/src/pages/friends.tsx
    - web/src/components/ParticipantEditor.tsx
  </read_first>
  <action>
Run and satisfy grep checks for migrated files:
- `rg -n "formatMoney\(" web/src/pages/friends.tsx web/src/pages/friend-public.tsx web/src/pages/group-public.tsx web/src/components/ParticipantEditor.tsx`
- `rg -n "text-green-600|text-red-500|text-red-600" web/src/pages/friends.tsx web/src/pages/friend-public.tsx web/src/pages/group-public.tsx web/src/components/ParticipantEditor.tsx`

Allow `formatMoney` only where non-money helper text is intentionally retained (target is zero in these files).
Keep non-money semantic color classes unrelated to amount text unchanged.
  </action>
  <acceptance_criteria>
    - no remaining `formatMoney(` calls in listed files for amount display paths
    - amount rows in listed files use `<MoneyValue`
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
rg -n "formatMoney\(|text-green-600|text-red-500|text-red-600" web/src/pages/friends.tsx web/src/pages/friend-public.tsx web/src/pages/group-public.tsx web/src/components/ParticipantEditor.tsx
cd web && npm run build
```
</verification>

<success_criteria>
- [ ] Friends/public pages use `MoneyValue` for displayed amounts.
- [ ] CompactEntityTable supports ReactNode secondary content.
- [ ] Public/friends currency fallback is explicit (`BRL`) where account currency is unavailable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-standardization-shared-value/01-05-SUMMARY.md`.
</output>
