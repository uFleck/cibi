---
phase: 01-ui-standardization-shared-value
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/ui/money-value.tsx
  - web/src/lib/format.ts
  - web/src/__tests__/money-value.test.tsx
autonomous: true
requirements:
  - UI-STD-01
  - UI-STD-03
  - UI-STD-04
  - UI-STD-06
must_haves:
  truths:
    - All money rendering primitives are centralized in a single reusable component.
    - Sign behavior is controlled by props (`showSign`) instead of ad-hoc string concatenation.
    - Tone behavior is controlled by props (`tone`) instead of repeated conditional Tailwind class logic.
  artifacts:
    - path: web/src/components/ui/money-value.tsx
      provides: Shared money display primitive
      contains: "export interface MoneyValueProps"
    - path: web/src/__tests__/money-value.test.tsx
      provides: Unit tests for sign/tone/currency contract
      contains: "describe('MoneyValue'"
    - path: web/src/lib/format.ts
      provides: Shared formatter compatibility used by MoneyValue
      contains: "export function formatMoney"
  key_links:
    - from: web/src/components/ui/money-value.tsx
      to: web/src/lib/format.ts
      via: formatMoney import
      pattern: "from '@/lib/format'"
---

<objective>
Create the shared value-render contract first so downstream file migrations are parallel-safe and low rework.

Purpose: Lock one API for amount/sign/tone/currency display.
Output: `MoneyValue` component + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ui-standardization-shared-value/01-RESEARCH.md
@.planning/phases/01-ui-standardization-shared-value/01-VALIDATION.md
@web/src/lib/format.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define MoneyValue contract and render behavior</name>
  <files>
    web/src/components/ui/money-value.tsx
  </files>
  <read_first>
    - web/src/lib/format.ts
    - web/src/components/ui/value-input.tsx
    - web/src/components/ui/card.tsx
  </read_first>
  <action>
Create `web/src/components/ui/money-value.tsx` exporting:
- `type MoneyTone = 'auto' | 'positive' | 'negative' | 'neutral'`
- `type MoneySign = 'auto' | 'always' | 'never'`
- `interface MoneyValueProps { amount: number; currency?: string; tone?: MoneyTone; showSign?: MoneySign; className?: string; }`

Implement concrete behavior:
- format text with `formatMoney(Math.abs(amount), currency)` to avoid double-sign bugs.
- sign policy:
  - `auto`: prepend `-` only when `amount < 0`
  - `always`: prepend `+` for `amount >= 0`, `-` for `amount < 0`
  - `never`: no `+`/`-` prefix
- tone policy:
  - `positive` => `text-green-600`
  - `negative` => `text-red-500`
  - `neutral` => `text-muted-foreground`
  - `auto` => amount-based (`>=0` green, `<0` red)
- always include `tabular-nums` in component className merge.
  </action>
  <acceptance_criteria>
    - `web/src/components/ui/money-value.tsx` exists
    - file contains `export interface MoneyValueProps`
    - file contains `tone?: 'auto' | 'positive' | 'negative' | 'neutral'`
    - file contains `showSign?: 'auto' | 'always' | 'never'`
    - file contains `tabular-nums`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add unit tests for sign/tone/currency contract</name>
  <files>
    web/src/__tests__/money-value.test.tsx
  </files>
  <read_first>
    - web/src/__tests__/dashboard.test.tsx
    - web/src/__tests__/verdict.test.tsx
    - web/src/components/ui/money-value.tsx
  </read_first>
  <action>
Create `web/src/__tests__/money-value.test.tsx` with Testing Library assertions for exact props behavior:
- `showSign='always'` with `amount={120}` renders leading `+`.
- `showSign='never'` with `amount={-120}` renders no sign prefix.
- `tone='positive'|'negative'|'neutral'` maps to `text-green-600|text-red-500|text-muted-foreground`.
- `currency='USD'` renders `$` formatting under `pt-BR` locale.
- default (`tone='auto'`, `showSign='auto'`) keeps negative sign for negative amount.
  </action>
  <acceptance_criteria>
    - file contains `describe('MoneyValue'`
    - file contains `showSign`
    - file contains `tone`
    - `cd web && npm test -- --run src/__tests__/money-value.test.tsx` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Keep formatter compatibility and export hygiene</name>
  <files>
    web/src/lib/format.ts
    web/src/components/ui/money-value.tsx
  </files>
  <read_first>
    - web/src/lib/format.ts
    - web/src/components/ui/money-value.tsx
  </read_first>
  <action>
Preserve existing `formatMoney` and `formatDate` exports in `web/src/lib/format.ts` (no breaking rename).
If helper extraction is needed, only add non-breaking exports; do not change existing `formatMoney(amount, currency='BRL')` signature.
Keep `MoneyValue` consuming `formatMoney` from `@/lib/format`.
  </action>
  <acceptance_criteria>
    - `web/src/lib/format.ts` still contains `export function formatMoney(amount: number, currency = 'BRL')`
    - `web/src/lib/format.ts` still contains `export function formatDate(isoString: string)`
    - `web/src/components/ui/money-value.tsx` contains `from '@/lib/format'`
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd web && npm test -- --run src/__tests__/money-value.test.tsx
cd web && npm run build
```
</verification>

<success_criteria>
- [ ] Shared `MoneyValue` contract exists and is importable.
- [ ] Sign and tone behavior are test-covered.
- [ ] Existing formatter API remains backward-compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-standardization-shared-value/01-02-SUMMARY.md`.
</output>
