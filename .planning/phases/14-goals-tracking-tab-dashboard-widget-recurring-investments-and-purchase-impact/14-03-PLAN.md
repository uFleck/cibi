---
phase: 14-goals-tracking-tab-dashboard-widget-recurring-investments-and-purchase-impact
plan: 03
type: execute
wave: 3
depends_on: [14-01, 14-02]
files_modified:
  - web/src/lib/api.ts
  - web/src/pages/goals.tsx
  - web/src/components/GoalsSnapshotWidget.tsx
  - web/src/components/GoalsSnapshotWidget.test.tsx
  - web/src/pages/goals.test.tsx
  - web/src/components/CompactEntityTable.tsx
  - web/src/components/StatCards.tsx
autonomous: true
requirements: [P14-01, P14-02, P14-03, P14-05]
must_haves:
  truths:
    - "Goals tab shows summary cards, urgency-first dense list, and recent activity block (D-01..D-04)."
    - "Dashboard shows top 3-5 urgent goals with progress+remaining and quick actions (D-05..D-08)."
    - "UI has guided empty state, skeleton loading, inline retry + toast, and updated cues (D-17..D-20)."
  artifacts:
    - path: web/src/pages/goals.tsx
      provides: sectioned goals tab
    - path: web/src/components/GoalsSnapshotWidget.tsx
      provides: dashboard multi-goal widget
    - path: web/src/pages/goals.test.tsx
      provides: state + ordering coverage
  key_links:
    - from: web/src/pages/goals.tsx
      to: /api/goals/tracking
      via: TanStack Query hook
      pattern: "\['goals','tracking'"
    - from: web/src/components/GoalsSnapshotWidget.tsx
      to: goals tab route
      via: quick action button
      pattern: "navigate\('/goals'"
---

<objective>
Build phase-visible UX for goals tracking in tab and dashboard widget, including robust state handling.

Purpose: Fulfill D-01..D-08 and D-17..D-20 with existing component patterns.
Output: goals page rewrite + snapshot widget + tests.
</objective>

<context>
@.planning/phases/14-goals-tracking-tab-dashboard-widget-recurring-investments-and-purchase-impact/14-CONTEXT.md
@web/src/lib/api.ts
@web/src/components/StatCards.tsx
@web/src/components/CompactEntityTable.tsx
@web/src/components/CheckWidget.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite Goals tab as sectioned tracking surface</name>
  <read_first>
    - web/src/pages/goals.tsx
    - web/src/components/CompactEntityTable.tsx
    - web/src/lib/api.ts
  </read_first>
  <action>Refactor goals.tsx to consume tracking + recurring APIs. Render sections in order per D-01: summary cards, dense goals list (progress bar + invested/target + remaining), recent global activity, and per-goal activity drill rows (D-04). Apply urgency-first ordering from API (D-03). For recurring entries/history, display source badges manual/system/recurring (D-12). Add empty guided CTA (D-17), skeleton progressive sections (D-18), inline retry + toast error handling (D-19), and "Updated {time}" cue after refetch/mutations (D-20).</action>
  <acceptance_criteria>grep -n "Updated" web/src/pages/goals.tsx && grep -n "Skeleton" web/src/pages/goals.tsx && cd web && npm test -- --run goals</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add GoalsSnapshotWidget and wire dashboard quick actions</name>
  <read_first>
    - web/src/components/StatCards.tsx
    - web/src/components/CheckWidget.tsx
    - web/src/pages/dashboard.tsx
  </read_first>
  <action>Create web/src/components/GoalsSnapshotWidget.tsx that shows top 3-5 urgent goals (D-05, D-08), each with percent progress + remaining amount (D-07), plus buttons: "Add contribution" and "Open Goals" (D-06). Reuse MoneyValue and compact row patterns for density. Mount widget on dashboard near StatCards/CheckWidget and keep responsive behavior consistent.</action>
  <acceptance_criteria>grep -n "GoalsSnapshotWidget" web/src/components/GoalsSnapshotWidget.tsx && grep -R "Open Goals" -n web/src && cd web && npm test -- --run goals-widget</acceptance_criteria>
</task>

</tasks>

<verification>
<automated>cd web && npm test -- --run goals goals-widget</automated>
</verification>

<success_criteria>
- Goals tab follows sectioned IA and urgency-first dense list.
- Dashboard shows multi-goal snapshot with both quick actions.
- Empty/loading/error/freshness cues implemented and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/14-goals-tracking-tab-dashboard-widget-recurring-investments-and-purchase-impact/14-03-SUMMARY.md`.
</output>
