---
phase: 14-goals-tracking-tab-dashboard-widget-recurring-investments-and-purchase-impact
plan: 04
type: execute
wave: 4
depends_on: [14-01, 14-03]
files_modified:
  - internal/service/engine.go
  - internal/service/engine_test.go
  - internal/handler/check.go
  - internal/handler/check_test.go
  - internal/handler/docs/openapi.yaml
  - web/src/lib/api.ts
  - web/src/components/CheckWidget.tsx
  - web/src/components/CheckWidget.test.tsx
  - web/src/pages/goals.tsx
autonomous: true
requirements: [P14-04, P14-05]
must_haves:
  truths:
    - "Purchase check preview shows per-goal impact before transaction confirm (D-13, D-16)."
    - "Each affected goal shows severity tier low/medium/high and amount/progress delta (D-14, D-15)."
    - "Check and goals surfaces refresh consistently after related mutations (D-20)."
  artifacts:
    - path: internal/service/engine.go
      provides: goal impact projection in check result
    - path: web/src/components/CheckWidget.tsx
      provides: impact preview block in verdict card
    - path: internal/handler/docs/openapi.yaml
      provides: /check impact contract parity
  key_links:
    - from: internal/handler/check.go
      to: internal/service/engine.go
      via: CheckResponse impact mapping
      pattern: "goal_impacts"
    - from: web/src/components/CheckWidget.tsx
      to: web/src/lib/api.ts
      via: CheckResponse type
      pattern: "goal_impacts"
---

<objective>
Integrate purchase impact projection into check flow and goals surfaces with verifiable API/runtime contract alignment.

Purpose: Deliver D-13..D-16 and close existing /check contract drift.
Output: /check impact payload + CheckWidget rendering + regression tests.
</objective>

<context>
@.planning/phases/14-goals-tracking-tab-dashboard-widget-recurring-investments-and-purchase-impact/14-CONTEXT.md
@internal/service/engine.go
@internal/handler/docs/openapi.yaml
@web/src/components/CheckWidget.tsx
@web/src/lib/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend engine/check response with per-goal impact projection</name>
  <read_first>
    - internal/service/engine.go
    - internal/handler/check.go
    - internal/handler/docs/openapi.yaml
  </read_first>
  <action>Extend EngineResult and check response mapping with goal_impacts[] entries: goal_id, goal_name, remaining_before, remaining_after, progress_before_pct, progress_after_pct, severity. Severity enum must be low|medium|high (D-15). Keep WAIT behavior and include WAIT in OpenAPI enum plus will_afford_after_payday/wait_until fields. Add engine + handler tests for BLOCKED/WAIT with impact payload present.</action>
  <acceptance_criteria>grep -n "goal_impacts" internal/handler/check.go && grep -n "WAIT" internal/handler/docs/openapi.yaml && go test ./internal/service ./internal/handler -run "Check|Impact|WAIT"</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render impact preview in CheckWidget and Goals tab context panels</name>
  <read_first>
    - web/src/components/CheckWidget.tsx
    - web/src/pages/goals.tsx
    - web/src/lib/api.ts
  </read_first>
  <action>Update CheckResponse TypeScript interface with goal_impacts array. In CheckWidget verdict state, render "Goal impact preview" section before reset button (D-16), listing impacted goals with severity badge + before/after remaining amount and progress deltas (D-13, D-14). Also show latest impact summary block in goals page when a check result exists or is returned from flow entry. Keep money rendering via MoneyValue only.</action>
  <acceptance_criteria>grep -n "Goal impact preview" web/src/components/CheckWidget.tsx && grep -n "goal_impacts" web/src/lib/api.ts && cd web && npm test -- --run CheckWidget goals</acceptance_criteria>
</task>

</tasks>

<verification>
<automated>go test ./internal/service ./internal/handler -run "Check|Impact|WAIT" && cd web && npm test -- --run CheckWidget goals</automated>
</verification>

<success_criteria>
- /check contract includes WAIT fields and goal_impacts payload.
- CheckWidget shows per-goal severity impact before confirmation action path.
- Backend + frontend tests verify impact rendering and WAIT compatibility.
</success_criteria>

<output>
After completion, create `.planning/phases/14-goals-tracking-tab-dashboard-widget-recurring-investments-and-purchase-impact/14-04-SUMMARY.md`.
</output>
