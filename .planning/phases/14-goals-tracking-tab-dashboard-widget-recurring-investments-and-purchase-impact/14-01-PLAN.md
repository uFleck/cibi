---
phase: 14-goals-tracking-tab-dashboard-widget-recurring-investments-and-purchase-impact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/service/goals_tracking.go
  - internal/service/goals_tracking_test.go
  - internal/handler/goals_tracking.go
  - internal/handler/goals_tracking_test.go
  - internal/handler/routes.go
  - internal/handler/docs/openapi.yaml
autonomous: true
requirements: [P14-01, P14-02, P14-05]
must_haves:
  truths:
    - "User can request one tracking payload with summary cards, urgency-sorted goals, and recent activity."
    - "Urgency sorting is deterministic and prefers nearest target date + larger remaining gap (D-03, D-08)."
    - "Tracking responses support loading/refresh UX with updated timestamp cue (D-20)."
  artifacts:
    - path: internal/service/goals_tracking.go
      provides: goals tracking DTO + urgency scoring
    - path: internal/handler/goals_tracking.go
      provides: GET /api/goals/tracking endpoint
    - path: internal/handler/docs/openapi.yaml
      provides: tracking endpoint contract
  key_links:
    - from: internal/handler/routes.go
      to: internal/handler/goals_tracking.go
      via: goals.GET("/tracking", ...)
      pattern: "goals\\.GET\\(\"/tracking\""
    - from: internal/handler/goals_tracking.go
      to: internal/service/goals_tracking.go
      via: service call
      pattern: "BuildTracking"
---

<objective>
Deliver backend tracking contracts for Goals tab and dashboard widget so frontend can consume one stable, urgency-first read model.

Purpose: Implement D-01..D-08 and D-20 at API/service level before UI.
Output: New /api/goals/tracking endpoint + tests + OpenAPI parity.
</objective>

<context>
@.planning/phases/14-goals-tracking-tab-dashboard-widget-recurring-investments-and-purchase-impact/14-CONTEXT.md
@.planning/phases/14-goals-tracking-tab-dashboard-widget-recurring-investments-and-purchase-impact/14-RESEARCH.md
@internal/handler/goals.go
@internal/handler/routes.go
@internal/handler/docs/openapi.yaml
@web/src/lib/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add goals tracking service contract and deterministic urgency sort</name>
  <read_first>
    - internal/service/goals.go
    - internal/repo/sqlite/goals.go
    - .planning/phases/14-goals-tracking-tab-dashboard-widget-recurring-investments-and-purchase-impact/14-CONTEXT.md
  </read_first>
  <action>Per D-01, D-03, D-04, D-05, D-07, D-08, create internal/service/goals_tracking.go with BuildTracking(accountID) returning: summary metrics, top_goals (max 5), and recent_activity. Implement urgency score using target_date proximity + remaining_amount gap; include stable tiebreakers (target_date asc, created_at asc). Include updated_at_utc field for D-20 freshness cues. Add unit tests in internal/service/goals_tracking_test.go for deterministic ordering and top-5 cap.</action>
  <acceptance_criteria>grep -n "type GoalsTrackingResponse" internal/service/goals_tracking.go && grep -n "SortStable" internal/service/goals_tracking.go && go test ./internal/service -run "GoalsTracking|Urgency"</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose GET /api/goals/tracking and align OpenAPI</name>
  <read_first>
    - internal/handler/goals.go
    - internal/handler/routes.go
    - internal/handler/docs/openapi.yaml
  </read_first>
  <action>Create internal/handler/goals_tracking.go with GET /api/goals/tracking?account_id=UUID. Validate account_id, call tracking service, return JSON with fields: summary, top_goals[], recent_activity[], updated_at_utc. Register route in routes.go. Update openapi.yaml with /goals/tracking schema and keep existing /goals contracts unchanged. Add handler tests for 200 and invalid account_id 400.</action>
  <acceptance_criteria>grep -n '/goals/tracking' internal/handler/docs/openapi.yaml && grep -n 'goals.GET("/tracking"' internal/handler/routes.go && go test ./internal/handler -run "GoalsTracking|Goals.*Tracking"</acceptance_criteria>
</task>

</tasks>

<verification>
<automated>go test ./internal/service ./internal/handler -run "GoalsTracking|Urgency|Tracking"</automated>
</verification>

<success_criteria>
- Tracking endpoint exists and returns urgency-first top goals.
- Payload contains summary + top_goals + recent_activity + updated_at_utc.
- OpenAPI includes /goals/tracking and tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/14-goals-tracking-tab-dashboard-widget-recurring-investments-and-purchase-impact/14-01-SUMMARY.md`.
</output>
