---
phase: 14-goals-tracking-tab-dashboard-widget-recurring-investments-and-purchase-impact
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - internal/repo/sqlite/migrations/0011_goal_recurring_contributions.sql
  - internal/repo/sqlite/goals.go
  - internal/service/goals_recurring.go
  - internal/service/goals_recurring_test.go
  - internal/handler/goals_recurring.go
  - internal/handler/routes.go
  - internal/handler/docs/openapi.yaml
  - web/src/lib/api.ts
autonomous: true
requirements: [P14-03, P14-05]
must_haves:
  truths:
    - "User can view due and overdue recurring goal contributions (D-09, D-11)."
    - "User must manually confirm each due contribution; no auto-post runs on read (D-10)."
    - "Posted recurring entries are tagged source=recurring and visible in history (D-12)."
  artifacts:
    - path: internal/service/goals_recurring.go
      provides: due/overdue classification and confirm-post flow
    - path: internal/handler/goals_recurring.go
      provides: recurring list + confirm endpoints
    - path: web/src/lib/api.ts
      provides: recurring goals client calls
  key_links:
    - from: internal/handler/goals_recurring.go
      to: internal/service/goals_recurring.go
      via: ListDue and ConfirmDue
      pattern: "ConfirmDue"
    - from: internal/service/goals_recurring.go
      to: goals ledger append
      via: AddLedgerEntry with source recurring
      pattern: "source.*recurring"
---

<objective>
Implement recurring goal contribution workflow with due/overdue visibility and manual confirm posting.

Purpose: Deliver D-09..D-12 with service-owned correctness and API/client contracts.
Output: recurring schedule storage + endpoints + tests + api client methods.
</objective>

<context>
@.planning/phases/14-goals-tracking-tab-dashboard-widget-recurring-investments-and-purchase-impact/14-CONTEXT.md
@internal/handler/goals.go
@internal/handler/routes.go
@internal/handler/docs/openapi.yaml
@web/src/lib/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add recurring contribution persistence and service logic</name>
  <read_first>
    - internal/repo/sqlite/migrations
    - internal/repo/sqlite/goals.go
    - internal/service/goals.go
  </read_first>
  <action>Create migration and repo methods for goal recurring contributions: goal_id, amount_cents, frequency, anchor_date_utc, next_due_utc, active. In service, implement ListRecurringDue(accountID, now) returning due/overdue items and executed history pointers; implement ConfirmRecurringDue(itemID, timestamp) that appends goal ledger entry with source=recurring and updates next_due_utc one period forward. Never auto-post on list calls (D-10). Add tests for overdue, selective posting, and no auto-catchup (D-11).</action>
  <acceptance_criteria>grep -R "source.*recurring" -n internal/service internal/repo/sqlite && go test ./internal/service -run "Goal.*Recurring|Recurring.*Due"</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add recurring endpoints, OpenAPI, and API client methods</name>
  <read_first>
    - internal/handler/goals.go
    - internal/handler/docs/openapi.yaml
    - web/src/lib/api.ts
  </read_first>
  <action>Add endpoints: GET /api/goals/recurring?account_id=... and POST /api/goals/recurring/{id}/confirm. Validate UUIDs, return consistent error JSON. Extend goal ledger source validation to accept recurring in goals handler and schema enum. Update openapi.yaml for new routes and source enum [manual, system, recurring]. Add web api.ts functions listGoalRecurring(accountId) and confirmGoalRecurring(id).</action>
  <acceptance_criteria>grep -n "goals/recurring" internal/handler/docs/openapi.yaml && grep -n "recurring'" web/src/lib/api.ts && go test ./internal/handler -run "Recurring|Goal.*Ledger.*Source"</acceptance_criteria>
</task>

</tasks>

<verification>
<automated>go test ./internal/service ./internal/handler -run "Recurring|Goal.*Ledger"</automated>
</verification>

<success_criteria>
- Due and overdue recurring items are listable by account.
- Confirm endpoint posts ledger entry with source=recurring and advances schedule.
- OpenAPI and TypeScript client include recurring routes and source enum.
</success_criteria>

<output>
After completion, create `.planning/phases/14-goals-tracking-tab-dashboard-widget-recurring-investments-and-purchase-impact/14-02-SUMMARY.md`.
</output>
