---
phase: 10-codebase-simplification-and-logic-centralization
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/pages/friends.tsx
  - web/src/components/FriendForm.tsx
  - web/src/components/DebtForm.tsx
  - web/src/components/GroupEventForm.tsx
  - web/src/components/ParticipantEditor.tsx
autonomous: true
requirements:
  - WEB-01
  - WEB-02
  - PEER-06
must_haves:
  truths:
    - friends.tsx is reduced to page orchestration, not giant inline form implementations
    - FriendForm, DebtForm, GroupEventForm, ParticipantEditor are extracted in web/src/components
    - Existing user-visible copy and interaction behavior are preserved exactly
  artifacts:
    - path: web/src/components/FriendForm.tsx
      provides: Friend create/edit form component with typed props
      contains: export interface FriendFormProps
    - path: web/src/components/DebtForm.tsx
      provides: Debt create/edit form component with installment controls
      contains: export interface DebtFormProps
    - path: web/src/components/GroupEventForm.tsx
      provides: Group event create/edit form component
      contains: export interface GroupEventFormProps
    - path: web/src/components/ParticipantEditor.tsx
      provides: Participant share editor component
      contains: export interface ParticipantEditorProps
---

<objective>
Execute D-05 for friends page: extract core form/editor blocks into focused reusable components while preserving current UI/UX contract and copy text.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-codebase-simplification-and-logic-centralization/10-CONTEXT.md
@.planning/phases/10-codebase-simplification-and-logic-centralization/10-UI-SPEC.md
@web/src/pages/friends.tsx
@web/src/components/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract FriendForm and DebtForm components from friends.tsx</name>
  <files>
    web/src/components/FriendForm.tsx
    web/src/components/DebtForm.tsx
    web/src/pages/friends.tsx
  </files>
  <read_first>
    - web/src/pages/friends.tsx
    - web/src/components/AppModal.tsx
    - .planning/phases/10-codebase-simplification-and-logic-centralization/10-UI-SPEC.md
  </read_first>
  <action>
Create `FriendForm.tsx` and `DebtForm.tsx` with explicit exported props interfaces.

Move existing inline JSX + handlers for:
- friend create/edit form,
- debt create form (including installment fields, frequency select).

In `friends.tsx`:
- keep query/mutation state in page,
- pass callbacks/data to new components via props,
- keep current string copy unchanged, including:
  - "Add Friend"
  - "Create Friend"
  - "Add"
  - "Cancel"
  - "Discard"

No API contract or mutation keys changed.
  </action>
  <acceptance_criteria>
    - web/src/components/FriendForm.tsx exists and contains `export interface FriendFormProps`
    - web/src/components/DebtForm.tsx exists and contains `export interface DebtFormProps`
    - web/src/pages/friends.tsx contains `import { FriendForm } from '@/components/FriendForm'`
    - web/src/pages/friends.tsx contains `import { DebtForm } from '@/components/DebtForm'`
    - web/src/pages/friends.tsx contains strings `Add Friend`, `Create Friend`, `Add`, `Cancel`, `Discard`
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extract GroupEventForm and ParticipantEditor from friends.tsx</name>
  <files>
    web/src/components/GroupEventForm.tsx
    web/src/components/ParticipantEditor.tsx
    web/src/pages/friends.tsx
  </files>
  <read_first>
    - web/src/pages/friends.tsx
    - web/src/components/ui/select.tsx
    - web/src/components/ui/input.tsx
    - .planning/phases/10-codebase-simplification-and-logic-centralization/10-UI-SPEC.md
  </read_first>
  <action>
Create `GroupEventForm.tsx` and `ParticipantEditor.tsx` extracted from existing inline sections.

Keep existing behavior for:
- event title/date/total amount/notes inputs,
- participant list and share edits,
- admin participant protection rule,
- toast flows and error strings.

Preserve exact copy strings:
- "New Event"
- "Create Event"
- "No participants set"
- "You (admin) must always stay in participants"

Do not change class names responsible for existing spacing and interaction states.
  </action>
  <acceptance_criteria>
    - web/src/components/GroupEventForm.tsx exists and contains `export interface GroupEventFormProps`
    - web/src/components/ParticipantEditor.tsx exists and contains `export interface ParticipantEditorProps`
    - web/src/pages/friends.tsx contains `import { GroupEventForm } from '@/components/GroupEventForm'`
    - web/src/pages/friends.tsx contains `import { ParticipantEditor } from '@/components/ParticipantEditor'`
    - web/src/pages/friends.tsx contains strings `New Event`, `Create Event`, `No participants set`
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Reduce friends.tsx to orchestration and preserve runtime behavior</name>
  <files>
    web/src/pages/friends.tsx
  </files>
  <read_first>
    - web/src/pages/friends.tsx
    - web/src/components/FriendForm.tsx
    - web/src/components/DebtForm.tsx
    - web/src/components/GroupEventForm.tsx
    - web/src/components/ParticipantEditor.tsx
  </read_first>
  <action>
Refactor friends page structure:
- Keep `useQuery` / `useMutation` / modal open state in page.
- Remove duplicated inline form JSX now represented by extracted components.
- Ensure all mutation invalidation keys remain identical.
- Ensure no new global context dependencies are introduced in extracted components.

Target outcome: friends.tsx acts as orchestrator only.
  </action>
  <acceptance_criteria>
    - web/src/pages/friends.tsx contains all four new component imports
    - web/src/pages/friends.tsx still contains query keys `['friends', accountId]` and `['peer-debts', accountId` strings
    - web/src/components/FriendForm.tsx does NOT contain `AccountContext`
    - web/src/components/DebtForm.tsx does NOT contain `AccountContext`
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI forms -> API calls | Input shape must remain unchanged after extraction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Tampering | form payload drift after refactor | mitigate | preserve mutation payload fields and query invalidation keys |
| T-10-12 | DoS/UX | double submit on modal forms | mitigate | keep existing disabled/loading patterns |
| T-10-13 | Integrity | participant editor state drift | mitigate | keep parent-owned state and callback props |
</threat_model>

<verification>
```bash
cd web && npm run build
cd web && npm test -- --run
```
</verification>

<success_criteria>
- [ ] 4 target components extracted from friends.tsx
- [ ] friends.tsx reduced to orchestration role
- [ ] UI copy and behavior unchanged
- [ ] Web build/tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-codebase-simplification-and-logic-centralization/10-04-SUMMARY.md`.
</output>
