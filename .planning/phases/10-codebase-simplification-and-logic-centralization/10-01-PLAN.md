---
phase: 10-codebase-simplification-and-logic-centralization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/repo/sqlite/peer_debt.go
  - internal/repo/sqlite/group_event.go
  - internal/repo/sqlite/peer_debt_test.go
  - internal/repo/sqlite/group_event_test.go
  - internal/engine/installment.go
  - internal/migrations/20260416000006_account_id_not_null.go
autonomous: true
requirements:
  - TXN-03
  - API-01
  - PEER-06
must_haves:
  truths:
    - Duplicate account-scoped/unscoped repo method pairs are replaced by single optional-account signatures
    - PeerDebtRepo.Update and GroupEventRepo.Update use one dynamic UPDATE statement each
    - Installment due-date math is centralized in internal/engine/NextInstallmentDue
    - Migration 20260416000006 enforces non-null account_id for PeerDebt and GroupEvent
  artifacts:
    - path: internal/repo/sqlite/peer_debt.go
      provides: Consolidated query methods + dynamic UPDATE builder
      contains: "func (r *SqlitePeerDebtRepo) GetAll(accountID *uuid.UUID)"
    - path: internal/repo/sqlite/group_event.go
      provides: Consolidated query methods + dynamic UPDATE builder
      contains: "func (r *SqliteGroupEventRepo) GetAll(accountID *uuid.UUID)"
    - path: internal/engine/installment.go
      provides: Shared installment next-due helper
      contains: "func NextInstallmentDue(firstDue time.Time, paidInstallments int64, frequency string) time.Time"
    - path: internal/migrations/20260416000006_account_id_not_null.go
      provides: NOT NULL enforcement migration
      contains: "ADD NOT NULL account_id enforcement for PeerDebt and GroupEvent"
---

<objective>
Implement the storage/repo simplification foundation for Phase 10:
1) Remove duplicate with-account / without-account repo paths by using one optional-account method per use-case.
2) Replace field-at-a-time UPDATE code with dynamic single-statement UPDATE builders.
3) Centralize installment next due-date logic into internal/engine.
4) Close schema integrity gap by enforcing account_id NOT NULL in follow-up migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-codebase-simplification-and-logic-centralization/10-CONTEXT.md
@.planning/phases/10-codebase-simplification-and-logic-centralization/10-RESEARCH.md
@.planning/PROJECT.md
@.planning/codebase/CONCERNS.md
@internal/repo/sqlite/peer_debt.go
@internal/repo/sqlite/group_event.go
@internal/engine/engine.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate PeerDebtRepo method pairs into optional-account signatures (D-01)</name>
  <files>
    internal/repo/sqlite/peer_debt.go
    internal/repo/sqlite/peer_debt_test.go
  </files>
  <read_first>
    - internal/repo/sqlite/peer_debt.go
    - internal/service/peer_debt.go
    - internal/handler/peer_debt.go
  </read_first>
  <action>
In `internal/repo/sqlite/peer_debt.go`, remove paired methods and keep one method per capability with `accountID *uuid.UUID` filter:

- `GetByFriend(friendID uuid.UUID, accountID *uuid.UUID) ([]PeerDebt, error)`
- `GetAll(accountID *uuid.UUID) ([]PeerDebt, error)`
- `GetBalanceByFriend(friendID uuid.UUID, accountID *uuid.UUID) (PeerDebtBalance, error)`
- `GetGlobalBalance(accountID *uuid.UUID) (GlobalPeerBalance, error)`
- `SumUpcomingPeerObligations(accountID *uuid.UUID, after, onOrBefore time.Time) (int64, error)`
- `SumNextUserPayment(accountID *uuid.UUID) (int64, error)`
- `GetActiveUserDebtsWithFriend(accountID *uuid.UUID) ([]ActiveUserDebt, error)`

SQL branch rule (exact): if `accountID != nil`, append `AND account_id = ?` (or `WHERE account_id = ?` for base SELECT).

Delete old pair methods:
`GetByFriendAndAccount`, `GetAllByAccount`, `GetBalanceByFriendAndAccount`, `GetGlobalBalanceByAccount`, `SumUpcomingPeerObligationsByAccount`, `SumNextUserPaymentByAccount`, `GetActiveUserDebtsWithFriendByAccount`.

Update repo tests to cover both `nil` and non-nil account paths.
  </action>
  <acceptance_criteria>
    - internal/repo/sqlite/peer_debt.go contains `func (r *SqlitePeerDebtRepo) GetAll(accountID *uuid.UUID)`
    - internal/repo/sqlite/peer_debt.go contains `if accountID != nil`
    - internal/repo/sqlite/peer_debt.go does NOT contain `GetAllByAccount(`
    - internal/repo/sqlite/peer_debt.go does NOT contain `GetByFriendAndAccount(`
    - `go test ./internal/repo/sqlite -run PeerDebt -count=1` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Consolidate GroupEventRepo method pairs into optional-account signatures (D-01)</name>
  <files>
    internal/repo/sqlite/group_event.go
    internal/repo/sqlite/group_event_test.go
  </files>
  <read_first>
    - internal/repo/sqlite/group_event.go
    - internal/service/group_event.go
    - internal/handler/group_event.go
  </read_first>
  <action>
In `internal/repo/sqlite/group_event.go`, consolidate paired methods into optional-account signatures:

- `GetAll(accountID *uuid.UUID) ([]GroupEvent, error)`
- `SumUpcomingAdminObligations(accountID *uuid.UUID, after, onOrBefore time.Time) (int64, error)`
- `GetAdminPendingExpenses(accountID *uuid.UUID) ([]AdminGroupExpense, error)`
- `GetPendingBalanceForAdmin(accountID *uuid.UUID) (GroupEventBalance, error)`

Delete old pair methods:
`GetAllByAccount`, `SumUpcomingAdminObligationsByAccount`, `GetAdminPendingExpensesByAccount`, `GetPendingBalanceForAdminByAccount`.

Preserve existing semantics and ordering; only remove duplication by parameterizing account scope.
  </action>
  <acceptance_criteria>
    - internal/repo/sqlite/group_event.go contains `func (r *SqliteGroupEventRepo) GetAll(accountID *uuid.UUID)`
    - internal/repo/sqlite/group_event.go contains `func (r *SqliteGroupEventRepo) SumUpcomingAdminObligations(accountID *uuid.UUID, after, onOrBefore time.Time)`
    - internal/repo/sqlite/group_event.go does NOT contain `GetAllByAccount(`
    - internal/repo/sqlite/group_event.go does NOT contain `GetAdminPendingExpensesByAccount(`
    - `go test ./internal/repo/sqlite -run GroupEvent -count=1` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Replace field-at-a-time updates with dynamic single-statement builders (D-03)</name>
  <files>
    internal/repo/sqlite/peer_debt.go
    internal/repo/sqlite/group_event.go
  </files>
  <read_first>
    - internal/repo/sqlite/peer_debt.go (Update)
    - internal/repo/sqlite/group_event.go (Update)
  </read_first>
  <action>
Implement the same pattern in both files:

1. Create local struct `type updateField struct { col string; val any }`.
2. Build `[]updateField` from non-nil patch args.
3. Build one SQL statement: `UPDATE <Table> SET col1 = ?, col2 = ? WHERE id = ?`.
4. Execute exactly one `Exec`.
5. If `RowsAffected == 0`, return wrapped `sql.ErrNoRows`.
6. If no fields provided, return explicit error `<repo>.Update: no fields provided`.

Remove row-by-row update transactions currently doing one UPDATE per field.
  </action>
  <acceptance_criteria>
    - internal/repo/sqlite/peer_debt.go contains `no fields provided`
    - internal/repo/sqlite/group_event.go contains `no fields provided`
    - internal/repo/sqlite/peer_debt.go contains `strings.Join(`
    - internal/repo/sqlite/group_event.go contains `strings.Join(`
    - internal/repo/sqlite/peer_debt.go does NOT contain `UPDATE PeerDebt SET amount = ? WHERE id = ?`
    - internal/repo/sqlite/group_event.go does NOT contain `UPDATE GroupEvent SET title = ? WHERE id = ?`
    - `go build ./...` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Centralize installment due-date logic and enforce NOT NULL account_id migration (D-04, D-06)</name>
  <files>
    internal/engine/installment.go
    internal/repo/sqlite/peer_debt.go
    internal/service/peer_debt.go
    internal/migrations/20260416000006_account_id_not_null.go
  </files>
  <read_first>
    - internal/engine/engine.go
    - internal/repo/sqlite/peer_debt.go
    - internal/service/peer_debt.go
    - internal/migrations/20260416000005_add_account_id_to_peer_and_group_tables.go
  </read_first>
  <action>
1) Add new file `internal/engine/installment.go` with:
`func NextInstallmentDue(firstDue time.Time, paidInstallments int64, frequency string) time.Time`.

Rules:
- weekly: `firstDue.AddDate(0, 0, int(paidInstallments)*7)`
- monthly/default: `firstDue.AddDate(0, int(paidInstallments), 0)`

2) Replace duplicated inline due-date blocks in:
- `internal/repo/sqlite/peer_debt.go`
- `internal/service/peer_debt.go`

3) Create migration `internal/migrations/20260416000006_account_id_not_null.go`.
- Enforce non-null `account_id` for PeerDebt and GroupEvent.
- Keep migration transactional.
- Preserve FK references and indexes.
  </action>
  <acceptance_criteria>
    - internal/engine/installment.go contains `func NextInstallmentDue(firstDue time.Time, paidInstallments int64, frequency string) time.Time`
    - internal/repo/sqlite/peer_debt.go contains `engine.NextInstallmentDue(`
    - internal/service/peer_debt.go contains `engine.NextInstallmentDue(`
    - internal/migrations/20260416000006_account_id_not_null.go exists
    - internal/migrations/20260416000006_account_id_not_null.go contains `PeerDebt`
    - internal/migrations/20260416000006_account_id_not_null.go contains `GroupEvent`
    - `go test ./...` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service -> repo/sqlite | Query semantics and filtering correctness across account scope |
| migration -> database | DDL changes can corrupt data if applied partially |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | account scope in consolidated queries | mitigate | Explicit `if accountID != nil` branch + tests for nil and non-nil |
| T-10-02 | Tampering | dynamic UPDATE builder | mitigate | Prepared placeholders only; no string interpolation of values |
| T-10-03 | DoS | migration failure mid-flight | mitigate | Execute migration inside tx; fail hard and rollback |
| T-10-04 | Integrity | hidden NULL account rows | mitigate | enforce NOT NULL and backfill checks before swap |
</threat_model>

<verification>
```bash
go test ./internal/repo/sqlite -count=1
go test ./internal/service -count=1
go test ./... -count=1
```
</verification>

<success_criteria>
- [ ] Repo duplication removed via optional-account signatures
- [ ] Dynamic UPDATE implemented in both target repos
- [ ] NextInstallmentDue exists and replaces duplicated logic
- [ ] 20260416000006 migration created and runnable
- [ ] Build and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-codebase-simplification-and-logic-centralization/10-01-SUMMARY.md`.
</output>
