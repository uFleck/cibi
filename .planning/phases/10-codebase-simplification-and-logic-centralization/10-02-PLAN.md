---
phase: 10-codebase-simplification-and-logic-centralization
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - internal/service/peer_debt.go
  - internal/service/group_event.go
  - internal/service/transactions.go
  - internal/service/friend.go
  - internal/handler/public.go
  - internal/handler/testhelpers_test.go
autonomous: true
requirements:
  - TXN-01
  - TXN-02
  - API-01
  - PEER-04
  - PEER-06
must_haves:
  truths:
    - Service interfaces no longer mirror duplicate account-scoped and unscoped methods
    - Public handler business logic is moved into service layer and handler becomes thin adapter
    - DeleteTransaction reverses account balance atomically using pre-delete transaction amount
    - RecordDebit dead path is either removed or corrected with non-inverted guard
  artifacts:
    - path: internal/service/transactions.go
      provides: DeleteTransaction atomic reversal and RecordDebit cleanup
      contains: "service.DeleteTransaction:"
    - path: internal/service/friend.go
      provides: Public friend view assembly for public handler
      contains: "GetPublicFriendView"
    - path: internal/handler/public.go
      provides: Thin HTTP adapter for public endpoints
      contains: "return c.JSON(http.StatusOK"
---

<objective>
Finish business-logic centralization and bug-fix layer:
1) align service contracts with consolidated repos,
2) move public endpoint orchestration logic from handler to service,
3) fix DeleteTransaction corruption bug,
4) resolve RecordDebit inverted-guard dead path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-codebase-simplification-and-logic-centralization/10-CONTEXT.md
@.planning/phases/10-codebase-simplification-and-logic-centralization/10-RESEARCH.md
@internal/service/transactions.go
@internal/service/friend.go
@internal/handler/public.go
@internal/handler/testhelpers_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align service methods to consolidated optional-account repo contracts (D-02)</name>
  <files>
    internal/service/peer_debt.go
    internal/service/group_event.go
    internal/handler/peer_debt.go
    internal/handler/group_event.go
    internal/handler/testhelpers_test.go
  </files>
  <read_first>
    - internal/service/peer_debt.go
    - internal/service/group_event.go
    - internal/repo/sqlite/peer_debt.go
    - internal/repo/sqlite/group_event.go
  </read_first>
  <action>
Update service + handler interface methods so they call consolidated repo methods with `accountID *uuid.UUID`.

Exact behavior:
- existing unscoped service methods pass `nil`
- account-scoped service methods pass `&accountID`

Do not keep duplicate repo-facing branches in service code.

Update compile-time assertions and mocks in `internal/handler/testhelpers_test.go` to the new signatures.
  </action>
  <acceptance_criteria>
    - internal/service/peer_debt.go contains `GetAll(nil)` and `GetAll(&accountID)`
    - internal/service/group_event.go contains `GetAll(nil)` and `GetAll(&accountID)`
    - internal/handler/testhelpers_test.go compiles with updated interfaces
    - `go test ./internal/handler -count=1` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Move GetFriendByToken business logic to service and thin public handler (D-07)</name>
  <files>
    internal/service/friend.go
    internal/handler/public.go
  </files>
  <read_first>
    - internal/handler/public.go
    - internal/service/friend.go
    - internal/service/group_event.go
    - internal/service/peer_debt.go
  </read_first>
  <action>
Create service-level orchestration API:

- add `type PublicFriendView struct` in `internal/service/friend.go`
- add `func (s *FriendService) GetPublicFriendView(token string) (PublicFriendView, error)`

Method must do all current handler orchestration:
- resolve friend by token
- fetch peer balance and debts
- fetch related group events + participants
- compute `viewerIsHost` and `hostedGroups`

Then simplify `internal/handler/public.go` `GetFriendByToken` to:
1) parse token,
2) HTML short-circuit unchanged,
3) call `friendSvc.GetPublicFriendView(token)`,
4) map service DTO to JSON response.

No loops over events/participants remain in handler.
  </action>
  <acceptance_criteria>
    - internal/service/friend.go contains `type PublicFriendView struct`
    - internal/service/friend.go contains `func (s *FriendService) GetPublicFriendView(token string)`
    - internal/handler/public.go does NOT contain `for _, e := range events`
    - internal/handler/public.go contains `GetPublicFriendView(token)`
    - `go test ./internal/service ./internal/handler -count=1` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Fix DeleteTransaction balance reversal atomically (D-08)</name>
  <files>
    internal/service/transactions.go
    internal/repo/sqlite/transactions.go
  </files>
  <read_first>
    - internal/service/transactions.go
    - internal/repo/sqlite/transactions.go
    - internal/repo/sqlite/accounts.go
  </read_first>
  <action>
Implement exact sequence in `DeleteTransaction`:
1. load transaction by id (for amount + account_id)
2. load account by transaction.account_id (for current_balance)
3. begin sql tx
4. delete transaction through repo using tx-aware method
5. compute `newBalance := acc.CurrentBalance - txn.Amount`
6. call `accRepo.UpdateBalance(txn.AccountID, newBalance, tx)`
7. commit

If `TransactionsRepo.DeleteByID` is not tx-aware, change signature to:
`DeleteByID(id uuid.UUID, tx *sql.Tx) error` and support nil fallback.
  </action>
  <acceptance_criteria>
    - internal/service/transactions.go contains `newBalance := acc.CurrentBalance - t.Amount`
    - internal/service/transactions.go contains `s.txnsRepo.DeleteByID(id, tx)`
    - internal/repo/sqlite/transactions.go contains `func (r *SqliteTxnsRepo) DeleteByID(id uuid.UUID, tx *sql.Tx) error`
    - `go test ./internal/service -run DeleteTransaction -count=1` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Resolve RecordDebit dead-path guard (D-09)</name>
  <files>
    internal/service/transactions.go
  </files>
  <read_first>
    - internal/service/transactions.go
    - internal/handler/transactions.go
    - web/src/pages/transactions.tsx
  </read_first>
  <action>
Audit callers of `RecordDebit`.

If no production callers exist:
- remove `RecordDebit` method and any now-unused code paths.

If production callers exist:
- keep method but fix guard to reject only when `next_occurrence` is not in future,
- preserve frequency advancement logic parity with `ConfirmRecurring`.

Do not keep inverted/dead behavior.
  </action>
  <acceptance_criteria>
    - either `internal/service/transactions.go` does NOT contain `func (s *TransactionsService) RecordDebit(`
    - or it contains guard `if !t.NextOccurrence.After(now)`
    - `go test ./internal/service -count=1` exits 0
    - `go build ./...` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handler -> service | Public endpoint data shaping and error mapping |
| service -> repo | Atomic account balance mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | DeleteTransaction | mitigate | one sql tx across delete + balance update |
| T-10-06 | Repudiation | recurring confirmation path | mitigate | keep deterministic guard + test coverage |
| T-10-07 | Information Disclosure | public handler | mitigate | keep 404 vs 500 mapping; no raw loop errors leaked |
</threat_model>

<verification>
```bash
go test ./internal/service -count=1
go test ./internal/handler -count=1
go build ./...
```
</verification>

<success_criteria>
- [ ] Service contracts match consolidated repos
- [ ] public.go is thin adapter
- [ ] DeleteTransaction performs atomic reversal
- [ ] RecordDebit dead/inverted path removed or fixed
- [ ] Build/tests green
</success_criteria>

<output>
After completion, create `.planning/phases/10-codebase-simplification-and-logic-centralization/10-02-SUMMARY.md`.
</output>
