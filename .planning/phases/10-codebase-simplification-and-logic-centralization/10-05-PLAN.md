---
phase: 10-codebase-simplification-and-logic-centralization
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/pages/accounts.tsx
  - web/src/pages/transactions.tsx
  - web/src/components/AccountForm.tsx
  - web/src/components/AccountScheduleForm.tsx
  - web/src/components/TransactionForm.tsx
  - web/src/components/TransactionFilters.tsx
autonomous: true
requirements:
  - WEB-01
  - WEB-02
  - TXN-01
  - TXN-02
must_haves:
  truths:
    - accounts.tsx and transactions.tsx are decomposed into focused form/filter components
    - Existing API behavior and query invalidation semantics remain unchanged
    - Existing copy strings and confirmation UX remain unchanged
  artifacts:
    - path: web/src/components/AccountForm.tsx
      provides: account create/edit form component
      contains: export interface AccountFormProps
    - path: web/src/components/AccountScheduleForm.tsx
      provides: pay schedule form component
      contains: export interface AccountScheduleFormProps
    - path: web/src/components/TransactionForm.tsx
      provides: transaction create/edit form component
      contains: export interface TransactionFormProps
    - path: web/src/components/TransactionFilters.tsx
      provides: transaction list filter/sort component
      contains: export interface TransactionFiltersProps
---

<objective>
Execute D-05 for remaining pages: split accounts.tsx and transactions.tsx into reusable components while preserving all current behavior and UI contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-codebase-simplification-and-logic-centralization/10-CONTEXT.md
@.planning/phases/10-codebase-simplification-and-logic-centralization/10-UI-SPEC.md
@web/src/pages/accounts.tsx
@web/src/pages/transactions.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract account page form components</name>
  <files>
    web/src/components/AccountForm.tsx
    web/src/components/AccountScheduleForm.tsx
    web/src/pages/accounts.tsx
  </files>
  <read_first>
    - web/src/pages/accounts.tsx
    - web/src/components/AppModal.tsx
    - web/src/components/ui/confirm-dialog.tsx
  </read_first>
  <action>
Extract two components:

1) `AccountForm.tsx`
- handles account create/edit fields (name, current_balance, currency)
- receives submit callback + loading state via props

2) `AccountScheduleForm.tsx`
- handles pay schedule create/edit fields (label, frequency, anchor_date, amount, day_of_month, day_of_month_2)
- receives validation errors and submit callback via props

In `accounts.tsx`, keep React Query calls and mutation invalidation keys unchanged.
Keep all current copy strings and toast messages unchanged.
  </action>
  <acceptance_criteria>
    - web/src/components/AccountForm.tsx exists and contains `export interface AccountFormProps`
    - web/src/components/AccountScheduleForm.tsx exists and contains `export interface AccountScheduleFormProps`
    - web/src/pages/accounts.tsx contains `import { AccountForm } from '@/components/AccountForm'`
    - web/src/pages/accounts.tsx contains `import { AccountScheduleForm } from '@/components/AccountScheduleForm'`
    - web/src/pages/accounts.tsx contains strings `Failed to create account`, `Failed to update account`, `Failed to delete account`
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extract transaction page form and filters components</name>
  <files>
    web/src/components/TransactionForm.tsx
    web/src/components/TransactionFilters.tsx
    web/src/pages/transactions.tsx
  </files>
  <read_first>
    - web/src/pages/transactions.tsx
    - web/src/components/ui/select.tsx
    - web/src/components/ui/switch.tsx
  </read_first>
  <action>
Extract two components from transactions page:

1) `TransactionForm.tsx`
- create/edit transaction fields
- recurring toggle/frequency/anchor inputs
- submit + validation props

2) `TransactionFilters.tsx`
- sort field and direction controls
- category/type filters
- filter reset control

In `transactions.tsx` keep:
- query keys/invalidation keys,
- confirm recurring flow (`confirmTransaction` mutation),
- current copy strings and button labels.
  </action>
  <acceptance_criteria>
    - web/src/components/TransactionForm.tsx exists and contains `export interface TransactionFormProps`
    - web/src/components/TransactionFilters.tsx exists and contains `export interface TransactionFiltersProps`
    - web/src/pages/transactions.tsx contains `import { TransactionForm } from '@/components/TransactionForm'`
    - web/src/pages/transactions.tsx contains `import { TransactionFilters } from '@/components/TransactionFilters'`
    - web/src/pages/transactions.tsx contains strings `Confirm Paid`, `Failed to confirm payment`, `Payment confirmed`
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Ensure page orchestrator responsibilities and state boundaries</name>
  <files>
    web/src/pages/accounts.tsx
    web/src/pages/transactions.tsx
    web/src/components/AccountForm.tsx
    web/src/components/TransactionForm.tsx
  </files>
  <read_first>
    - web/src/pages/accounts.tsx
    - web/src/pages/transactions.tsx
  </read_first>
  <action>
Finalize decomposition boundaries:
- Keep data-fetching and mutation hooks in page files.
- Keep modal open/close state and selected ids in page files.
- Keep extracted components prop-driven and reusable.
- Do not introduce new global context usage inside extracted form components.

Run full web build and tests after refactor.
  </action>
  <acceptance_criteria>
    - web/src/components/AccountForm.tsx does NOT contain `useQuery(`
    - web/src/components/TransactionForm.tsx does NOT contain `useQuery(`
    - web/src/components/TransactionForm.tsx does NOT contain `useContext(AccountContext)`
    - web/src/pages/accounts.tsx contains `useQuery({` and `useMutation({`
    - web/src/pages/transactions.tsx contains `useQuery({` and `useMutation({`
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page state -> extracted components | Must preserve payload mapping and validation behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-14 | Tampering | account form payload after extraction | mitigate | keep page-owned mutation + exact payload keys |
| T-10-15 | Tampering | transaction confirm path | mitigate | keep existing confirmTransaction mutation path unchanged |
| T-10-16 | DoS/UX | filter state causing unnecessary refetches | mitigate | keep filters client-side and query key unchanged |
</threat_model>

<verification>
```bash
cd web && npm run build
cd web && npm test -- --run
```
</verification>

<success_criteria>
- [ ] accounts.tsx decomposition complete
- [ ] transactions.tsx decomposition complete
- [ ] copy and behavior unchanged
- [ ] web build/tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-codebase-simplification-and-logic-centralization/10-05-SUMMARY.md`.
</output>
