---
phase: 13-goals-foundation-goal-entity-target-amount-and-investment-ledger
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - web/src/lib/api.ts
  - web/src/pages/goals.tsx
  - web/src/router.tsx
  - web/src/__tests__/goals.test.tsx
autonomous: true
requirements: ["TBD"]
must_haves:
  truths:
    - "User can create a goal using decimal inputs and see persisted values rendered as money."
    - "User can add contribution/withdrawal/adjustment entries and see updated invested progress."
    - "User sees validation errors for insufficient funds without UI crashes."
  artifacts:
    - path: "web/src/lib/api.ts"
      provides: "Goal/ledger API types and client functions"
    - path: "web/src/pages/goals.tsx"
      provides: "Phase-13 supporting goal management UI flow"
    - path: "web/src/__tests__/goals.test.tsx"
      provides: "Frontend regression tests for core goal mutation flow"
  key_links:
    - from: "web/src/pages/goals.tsx"
      to: "web/src/lib/api.ts"
      via: "TanStack Query mutations + invalidation"
      pattern: "useMutation|invalidateQueries"
    - from: "web/src/lib/api.ts"
      to: "/api/goals"
      via: "fetch wrappers"
      pattern: "apiFetch<.*>\('/api/goals"
---

<objective>
Add minimal web supporting flows for goals foundation (no Phase-14 dashboard/tab scope creep).

Purpose: make Phase-13 backend capabilities operable from UI and validated end-to-end.
Output: goal create + ledger entry interaction flow + tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-goals-foundation-goal-entity-target-amount-and-investment-ledger/13-CONTEXT.md
@web/src/lib/api.ts
@web/src/pages/transactions.tsx
@web/src/components/ui/money-value.tsx
@web/src/lib/format.ts

<interfaces>
From web/src/lib/api.ts:
```ts
async function apiFetch<T>(path: string, options?: RequestInit): Promise<T>
```

From backend contract in plan 02:
```text
POST /api/goals
POST /api/goals/:id/ledger
POST /api/goals/:id/ledger/:entryId/reverse
GET  /api/goals
GET  /api/goals/:id/ledger
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend web API client and typed contracts for goals endpoints</name>
  <read_first>
    - web/src/lib/api.ts
    - .planning/phases/13-goals-foundation-goal-entity-target-amount-and-investment-ledger/13-02-PLAN.md
    - .planning/codebase/CONVENTIONS.md
  </read_first>
  <files>web/src/lib/api.ts</files>
  <behavior>
    - Test 1: API type definitions map snake_case backend fields and enum unions.
    - Test 2: create/update goal payload uses decimal numbers; response parsing mirrors existing API helpers.
    - Test 3: ledger reverse operation uses dedicated endpoint (no delete helper).
  </behavior>
  <action>Add `GoalResponse`, `GoalLedgerEntryResponse`, request types, and API functions for list/create/update goals + list/add/reverse ledger entries. Keep error propagation via existing `apiFetch` pattern; include `code` passthrough for insufficient funds messaging.</action>
  <acceptance_criteria>
    - `rg -n "interface GoalResponse|GoalLedgerEntryResponse|createGoal|addGoalLedgerEntry|reverseGoalLedgerEntry" web/src/lib/api.ts` finds new API surface
    - `rg -n "contribution|withdrawal|adjustment|manual|system" web/src/lib/api.ts` finds enum usage
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm test -- --run goals</automated>
  </verify>
  <done>Frontend has typed API client coverage for all Phase-13 goal endpoints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build minimal goals page flow + tests (respect Phase-14 defer)</name>
  <read_first>
    - .planning/phases/13-goals-foundation-goal-entity-target-amount-and-investment-ledger/13-CONTEXT.md
    - web/src/pages/transactions.tsx
    - web/src/components/ui/money-value.tsx
  </read_first>
  <files>web/src/pages/goals.tsx, web/src/router.tsx, web/src/__tests__/goals.test.tsx</files>
  <behavior>
    - Test 1: user creates goal with decimal target, request sends decimal and UI renders monetary values via MoneyValue/format helpers.
    - Test 2: user posts contribution and sees refreshed invested/progress data.
    - Test 3: insufficient-funds contribution shows error toast/message and does not corrupt list state.
  </behavior>
  <action>Create a simple goals page/route with: account-scoped goals list, create goal form, add ledger entry form, and reverse action trigger. Explicitly avoid deferred items (no dashboard widget, no purchase-impact simulation, no recurring investment automation). Use TanStack Query invalidation on successful mutations.</action>
  <acceptance_criteria>
    - `rg -n "createGoal|addGoalLedgerEntry|reverseGoalLedgerEntry|useMutation|invalidateQueries" web/src/pages/goals.tsx` finds wiring
    - `rg -n "Test.*goal|insufficient|contribution|withdrawal" web/src/__tests__/goals.test.tsx` finds regression scenarios
    - `cd web && npm test -- --run goals` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm test -- --run goals</automated>
  </verify>
  <done>Phase-13 UI flow is functional and test-backed without leaking into Phase-14 scope.</done>
</task>

</tasks>

<verification>
- `cd web && npm test -- --run goals`
- `cd web && npm run build`
- `go test ./internal/handler -run Goals -count=1`
</verification>

<success_criteria>
- User can operate goals foundation via UI (create goal, post ledger entries, reverse corrections).
- UI uses money formatting conventions and preserves backend validation semantics.
- No deferred Phase-14 UX features are implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/13-goals-foundation-goal-entity-target-amount-and-investment-ledger/13-03-SUMMARY.md`
</output>
