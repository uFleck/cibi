---
phase: 13-goals-foundation-goal-entity-target-amount-and-investment-ledger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/migrations/20260429000006_goals_foundation.go
  - internal/repo/sqlite/goals.go
  - internal/repo/sqlite/goals_test.go
  - internal/service/goals.go
  - internal/service/goals_test.go
  - internal/app/app.go
autonomous: true
requirements: ["TBD"]
must_haves:
  truths:
    - "User can create an account-scoped goal with target amount in cents and lifecycle status."
    - "User can add goal ledger entries and account balance is updated atomically in the same transaction."
    - "User cannot contribute more than available account balance."
    - "Goal auto-completes when invested total reaches or exceeds target."
  artifacts:
    - path: "internal/migrations/20260429000006_goals_foundation.go"
      provides: "Goal + goal_ledger schema with constraints, enums/checks, and audit fields"
    - path: "internal/repo/sqlite/goals.go"
      provides: "Goal/ledger repository contracts and sqlite implementation with tx-aware writes"
    - path: "internal/service/goals.go"
      provides: "Business rules for D-01..D-16 including atomic account coupling"
  key_links:
    - from: "internal/service/goals.go"
      to: "internal/repo/sqlite/goals.go"
      via: "service methods call repo tx-aware write methods"
      pattern: "Begin\(|tx\.Commit\(|AddLedgerEntry"
    - from: "internal/service/goals.go"
      to: "internal/repo/sqlite/accounts.go"
      via: "UpdateBalance in same tx as ledger insert"
      pattern: "UpdateBalance\(.*tx\)"
---

<objective>
Build the backend goals foundation (schema + repo + service) with immutable investment ledger and atomic account-balance coupling.

Purpose: enforce money correctness + auditability for Phase 14 features.
Output: production-ready domain layer for goals and ledger mutations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-goals-foundation-goal-entity-target-amount-and-investment-ledger/13-CONTEXT.md
@.planning/phases/13-goals-foundation-goal-entity-target-amount-and-investment-ledger/13-RESEARCH.md
@internal/service/transactions.go
@internal/repo/sqlite/transactions.go

<interfaces>
From internal/service/transactions.go:
```go
type TransactionsService struct {
    db       *sql.DB
    txnsRepo sqlite.TransactionsRepo
    accRepo  sqlite.AccountsRepo
}
```

From internal/repo/sqlite/transactions.go pattern:
```go
func (r *SqliteTxnsRepo) Insert(t Transaction, tx *sql.Tx) error
func (r *SqliteTxnsRepo) Update(id uuid.UUID, upd UpdateTransaction, tx *sql.Tx) error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create goals schema + repo contracts (per D-01..D-12)</name>
  <read_first>
    - .planning/phases/13-goals-foundation-goal-entity-target-amount-and-investment-ledger/13-CONTEXT.md
    - .planning/codebase/CONVENTIONS.md
    - internal/migrations/20260416000005_account_scoped_friend_ledger.go
  </read_first>
  <files>internal/migrations/20260429000006_goals_foundation.go, internal/repo/sqlite/goals.go, internal/repo/sqlite/goals_test.go</files>
  <behavior>
    - Test 1: goal insert persists required fields (`name`, `account_id`, `target_amount_cents`, `start_date_utc`) and initial status in allowed lifecycle.
    - Test 2: ledger insert accepts only `contribution|withdrawal|adjustment` and `source` `manual|system`.
    - Test 3: ledger rows are append-only (no hard-delete method exposed in repo API).
  </behavior>
  <action>Create migration + repo for Goal and GoalLedger per D-01/D-02/D-03/D-09/D-10/D-11/D-12. Include goal target audit table/history columns so D-05 can be implemented now. Ensure account_id FK, UTC/RFC3339 fields, integer cents only, and optional reverses_entry_id trace for reversal chain. Write repo tests first, then implementation.</action>
  <acceptance_criteria>
    - `rg -n "type Goal struct|type GoalLedgerEntry struct|type GoalsRepo interface" internal/repo/sqlite/goals.go` finds contracts
    - `rg -n "contribution|withdrawal|adjustment|manual|system" internal/migrations/20260429000006_goals_foundation.go` finds enforced enum/checks
    - `go test ./internal/repo/sqlite -run Goals -count=1` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/repo/sqlite -run Goals -count=1</automated>
  </verify>
  <done>Schema and repo layer enforce locked data contract and immutable ledger primitives.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement atomic GoalsService ledger/balance workflows (per D-13..D-16)</name>
  <read_first>
    - internal/service/transactions.go
    - .planning/phases/13-goals-foundation-goal-entity-target-amount-and-investment-ledger/13-CONTEXT.md
    - internal/repo/sqlite/goals.go
  </read_first>
  <files>internal/service/goals.go, internal/service/goals_test.go, internal/app/app.go</files>
  <behavior>
    - Test 1: contribution debits account balance atomically with ledger insert.
    - Test 2: withdrawal credits account balance atomically with ledger insert.
    - Test 3: insufficient funds blocks contribution with validation error (D-15).
    - Test 4: invested_total >= target transitions goal to completed (D-04), overfunding remains completed (D-06).
    - Test 5: target update writes audit trail and recomputes completion status (D-05).
  </behavior>
  <action>Add GoalsService using `*sql.DB` transaction pattern from TransactionsService; wire in app.New(). Enforce D-07 currency match against account, D-08 half-up conversion at boundary contracts, D-13/D-14 balance effects, D-16 atomicity for ledger+balance+goal aggregate. Provide explicit reverse-adjustment operation instead of delete (D-10).</action>
  <acceptance_criteria>
    - `rg -n "type GoalsService struct|func NewGoalsService|func \(s \*GoalsService\) AddLedgerEntry" internal/service/goals.go` finds service surface
    - `rg -n "insufficient|currency|completed|revers" internal/service/goals_test.go` finds rule coverage
    - `go test ./internal/service -run Goals -count=1` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/service -run Goals -count=1</automated>
  </verify>
  <done>Service layer guarantees money correctness and lifecycle transitions under one DB transaction.</done>
</task>

</tasks>

<verification>
- `go test ./internal/repo/sqlite -run Goals -count=1`
- `go test ./internal/service -run Goals -count=1`
- `go test ./... -run Goals -count=1`
</verification>

<success_criteria>
- Backend has account-scoped goals + immutable ledger primitives.
- Contributions/withdrawals update account balances atomically with ledger rows.
- No hard-delete ledger path exists; reversal flow is available.
</success_criteria>

<output>
After completion, create `.planning/phases/13-goals-foundation-goal-entity-target-amount-and-investment-ledger/13-01-SUMMARY.md`
</output>
