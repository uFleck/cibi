---
phase: 13-goals-foundation-goal-entity-target-amount-and-investment-ledger
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - internal/handler/goals.go
  - internal/handler/goals_test.go
  - internal/handler/routes.go
  - internal/app/app.go
  - internal/handler/docs/openapi.yaml
autonomous: true
requirements: ["TBD"]
must_haves:
  truths:
    - "User can create/list/update goals via API with account scoping and cents-safe conversion."
    - "User can add contribution/withdrawal/adjustment entries via API and receives consistent validation errors."
    - "Correction flow uses reversal entry API instead of delete endpoint."
  artifacts:
    - path: "internal/handler/goals.go"
      provides: "Goal + ledger HTTP handlers"
    - path: "internal/handler/routes.go"
      provides: "Registered /api/goals routes"
    - path: "internal/handler/goals_test.go"
      provides: "Handler contract and validation coverage"
  key_links:
    - from: "internal/handler/goals.go"
      to: "internal/service/goals.go"
      via: "GoalsServiceIface"
      pattern: "type GoalsServiceIface interface"
    - from: "internal/handler/routes.go"
      to: "internal/handler/goals.go"
      via: "api group route registration"
      pattern: "api\.Group\(\"/goals\"\)"
---

<objective>
Expose goals foundation through REST API with strict validation and decimal↔cents conversion boundaries.

Purpose: unblock frontend and keep business rules centralized in service layer.
Output: `/api/goals` and `/api/goals/:id/ledger` endpoints + tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-goals-foundation-goal-entity-target-amount-and-investment-ledger/13-01-PLAN.md
@.planning/phases/13-goals-foundation-goal-entity-target-amount-and-investment-ledger/13-CONTEXT.md
@internal/handler/transactions.go
@internal/handler/routes.go
@internal/handler/errors.go

<interfaces>
From internal/handler/transactions.go pattern:
```go
type TransactionsServiceIface interface { ... }
func NewTransactionsHandler(svc *service.TransactionsService) *TransactionsHandler
```

From internal/handler/routes.go pattern:
```go
api := e.Group("/api")
txn := api.Group("/transactions")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add goals handler API surface and route wiring (per D-01..D-12)</name>
  <read_first>
    - internal/handler/transactions.go
    - internal/handler/routes.go
    - .planning/phases/13-goals-foundation-goal-entity-target-amount-and-investment-ledger/13-CONTEXT.md
  </read_first>
  <files>internal/handler/goals.go, internal/handler/routes.go, internal/app/app.go</files>
  <behavior>
    - Test 1: create goal endpoint converts decimal target to cents and returns API shape.
    - Test 2: ledger contribution endpoint validates type/source enums and maps service errors to JSON `error`.
    - Test 3: no DELETE ledger endpoint is registered; reversal endpoint exists.
  </behavior>
  <action>Create `GoalsHandler` + local `GoalsServiceIface`, following compile-time assertion pattern. Implement endpoints for create/list/update goals, create ledger entry, reverse entry, and list ledger by goal. Route under `/api/goals` and `/api/goals/:id/ledger` per discretion while honoring D-10 (no hard delete). Keep handlers thin; all rules live in service.</action>
  <acceptance_criteria>
    - `rg -n "type GoalsHandler struct|type GoalsServiceIface interface|func NewGoalsHandler" internal/handler/goals.go` finds handler scaffold
    - `rg -n "/api/goals|/goals/:id/ledger|reverse" internal/handler/routes.go` finds route registration
    - `go test ./internal/handler -run Goals -count=1` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/handler -run Goals -count=1</automated>
  </verify>
  <done>API surface exists and is wired with conventions-compliant validation/error behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add handler tests + OpenAPI updates for goals contracts</name>
  <read_first>
    - internal/handler/testhelpers_test.go
    - internal/handler/errors_test.go
    - internal/handler/docs/openapi.yaml
  </read_first>
  <files>internal/handler/goals_test.go, internal/handler/docs/openapi.yaml</files>
  <behavior>
    - Test 1: invalid payload (negative/zero target, bad enum) returns 400 with standard error body.
    - Test 2: insufficient-funds contribution maps to validation error and preserves structured code if present.
    - Test 3: successful create/ledger requests return dollars in JSON while backend works in cents.
  </behavior>
  <action>Add focused handler tests using existing `serveRequest`/mock patterns. Extend OpenAPI docs for new goals and ledger endpoints, including request/response schemas and enum values from D-02/D-09/D-11.</action>
  <acceptance_criteria>
    - `rg -n "TestGoals|insufficient|reverse|validation" internal/handler/goals_test.go` finds coverage
    - `rg -n "/api/goals|goal ledger|contribution|withdrawal|adjustment" internal/handler/docs/openapi.yaml` finds docs entries
    - `go test ./internal/handler -run Goals -count=1` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/handler -run Goals -count=1</automated>
  </verify>
  <done>Goal API contract is executable, documented, and regression-tested.</done>
</task>

</tasks>

<verification>
- `go test ./internal/handler -run Goals -count=1`
- `go test ./... -run Goals -count=1`
</verification>

<success_criteria>
- REST endpoints cover goal lifecycle and ledger mutation flows.
- Validation and error responses follow existing platform conventions.
- OpenAPI includes all new endpoint contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/13-goals-foundation-goal-entity-target-amount-and-investment-ledger/13-02-SUMMARY.md`
</output>
